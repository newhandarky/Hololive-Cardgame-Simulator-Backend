# 重構進度追蹤

更新日期：2026-04-24
定位：系統優化執行紀錄與體積變化追蹤
用途：每完成一個優化步驟，就更新一次目前已完成項目、檔案體積變化、驗證狀態與下一步。

---

## 一、追蹤規則

本文件採用以下原則維護：

1. 每完成一個明確的優化步驟，就新增一筆紀錄
2. 每筆紀錄至少包含：
   - 步驟名稱
   - 修改範圍
   - 影響檔案
   - 體積變化
   - 驗證結果
   - 下一步
3. 體積變化優先記錄下列大檔：
   - `MatchEffectService.java`
   - `MatchActionService.java`
   - `HardNpcService.java`
   - `DeckService.java`
4. 若當次步驟未影響某個大檔，仍保留最新尺寸，方便持續比較

---

## 二、基準尺寸

以下基準用來觀察本輪重構是否真的讓 God Class 下降，而不是只把複雜度搬家。

### 本輪追蹤基準

- `MatchEffectService.java`：`13,717` 行，`549,235 bytes`
- `MatchActionService.java`：`10,248` 行，`408,040 bytes`
- `HardNpcService.java`：`1,270` 行，`48,661 bytes`
- `DeckService.java`：`857` 行，`33,603 bytes`

---

## 三、目前尺寸

截至本次更新：

- `MatchEffectService.java`：`12,054` 行，`485,281 bytes`
- `MatchActionService.java`：`9,013` 行，`354,520 bytes`
- `MatchEffectDamageService.java`：`103` 行，`2,904 bytes`
- `MatchTriggeredCardEffectService.java`：`141` 行，`5,108 bytes`
- `MatchGiftTriggerService.java`：`705` 行，`24,229 bytes`
- `MatchGiftTriggerContextService.java`：`252` 行，`10,407 bytes`
- `MatchTriggeredGiftResolutionService.java`：`311` 行，`13,794 bytes`
- `MatchTriggeredEffectResolutionService.java`：`254` 行，`10,925 bytes`
- `HardNpcService.java`：`1,270` 行，`48,661 bytes`
- `DeckService.java`：`857` 行，`33,603 bytes`

---

## 四、進度紀錄

## Step E2-1

日期：2026-04-24
狀態：已完成
主題：抽出 Gift trigger orchestration 與獨立入口 service

### 本步完成內容

- 新增 `MatchGiftTriggerOrchestrationService`
- 新增 `MatchGiftTriggerEligibilityService`
- 新增 `MatchGiftTriggerSourceContext`
- 新增 `MatchGiftTriggerService`
- 將 Gift trigger 的對外入口從 `MatchEffectService` 搬到 `MatchGiftTriggerService`
- 將 `MatchActionService` 與 gift 相關測試改成直接呼叫 `MatchGiftTriggerService`
- `MatchEffectService` 保留 Gift 通用執行能力與少量 callback / condition wrapper，不再承接整段 trigger facade

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchGiftTriggerService.java`
- `src/main/java/com/hololive/cardgame/service/MatchGiftTriggerOrchestrationService.java`
- `src/main/java/com/hololive/cardgame/service/MatchGiftTriggerEligibilityService.java`
- `src/main/java/com/hololive/cardgame/service/MatchGiftTriggerSourceContext.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchEffectService.java`
  - `13,717` 行 -> `12,712` 行
  - `549,235 bytes` -> `511,635 bytes`
  - 淨減少 `1,005` 行，`37,600 bytes`
- `MatchActionService.java`
  - `10,248` 行 -> `10,251` 行
  - `408,040 bytes` -> `408,308 bytes`
  - 淨增加 `3` 行，`268 bytes`

### 判讀

- 這一步的重點是把 Gift trigger 從 `MatchEffectService` 的 facade 層切出去
- `MatchEffectService` 已經開始明顯瘦身
- `MatchActionService` 目前還在改依賴注入與 call site，尚未進入真正瘦身階段

### 驗證結果

- gift 相關新舊檔案的局部 `javac` 編譯可通過
- `./mvnw -q -DskipTests compile` 目前仍被 repo 內既有的 `LobbyMatchService` / DTO / entity 編譯問題阻塞，非這一步新增的回歸
- 本步未執行 integration test

### 下一步

- 繼續拆 Gift trigger 的狀態與快照管理
- 目標是把 `recordPerformancePhaseSnapshot` 這類 Gift trigger 專屬狀態操作也從 `MatchEffectService` 移出
- 完成後，再考慮是否讓 `MatchGiftTriggerService` 進一步吸收更多 Gift 專屬 helper

---

## Step E2-2

日期：2026-04-24
狀態：已完成
主題：將 performance phase snapshot 寫入移入 Gift trigger 模組

### 本步完成內容

- 將 `recordPerformancePhaseSnapshot(...)` 從 `MatchEffectService` 移到 `MatchGiftTriggerService`
- 將 `MatchActionService` 在進入 `PERFORMANCE` 前的 snapshot 寫入改成直接呼叫 `MatchGiftTriggerService`
- 讓 Gift trigger 的 `PERFORMANCE_SNAPSHOT` 狀態責任更集中：
  - 寫入端在 `MatchGiftTriggerService`
  - 讀取判斷端在 `MatchGiftTriggerConditionService`
- `MatchEffectService` 不再承擔 Gift trigger 專屬的 performance snapshot 基礎設施

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchGiftTriggerService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`

### 體積變化

- `MatchEffectService.java`
  - `12,712` 行 -> `12,633` 行
  - `511,635 bytes` -> `509,239 bytes`
  - 淨減少 `79` 行，`2,396 bytes`
- `MatchActionService.java`
  - `10,251` 行 -> `10,256` 行
  - `408,308 bytes` -> `408,416 bytes`
  - 淨增加 `5` 行，`108 bytes`
- `MatchGiftTriggerService.java`
  - `826` 行 -> `827` 行
  - `29,034 bytes` -> `29,101 bytes`
  - 淨增加 `1` 行，`67 bytes`

### 判讀

- 這一步沒有大幅瘦身，但有把 Gift trigger 的狀態讀寫邊界補齊
- `MatchEffectService` 再少掉一段和 Gift 專屬狀態管理相關的責任
- `MatchGiftTriggerService` 現在不只負責 trigger 入口，也開始承接 trigger lifecycle 需要的狀態寫入

### 驗證結果

- `MatchGiftTriggerService`、`MatchGiftTriggerConditionService`、`MatchEffectService` 的局部 `javac` 編譯可通過
- `./mvnw -q -DskipTests compile` 仍受 repo 內既有的 `LobbyMatchService` / DTO / entity 編譯問題阻塞，非本步新增
- 本步未執行 integration test

### 下一步

- 繼續盤點 `MatchEffectService` 內剩餘的 Gift 專屬 helper
- 優先看是否能把 Gift trigger 專屬的 payload/context 組裝再往 `MatchGiftTriggerService` 收斂
- 如果這條線的邊界已足夠穩，再轉去拆 `MatchActionService.resolveDecision(...)`

---

## Step E2-3

日期：2026-04-24
狀態：已完成
主題：抽出 Gift runtime context helper

### 本步完成內容

- 新增 `MatchGiftTriggerContextService`
- 將下列 Gift 專屬上下文處理抽出到新 helper：
  - `recordPerformancePhaseSnapshot(...)`
  - `loadGiftHolderSnapshot(...)`
  - `appendStoredGiftExecutionContext(...)`
- `MatchGiftTriggerService` 改為委派 `MatchGiftTriggerContextService` 處理 snapshot 與 holder snapshot
- `MatchEffectService` 改為委派 `MatchGiftTriggerContextService` 處理 stored trigger payload context 組裝

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchGiftTriggerContextService.java`
- `src/main/java/com/hololive/cardgame/service/MatchGiftTriggerService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`

### 體積變化

- `MatchEffectService.java`
  - `12,633` 行 -> `12,582` 行
  - `509,239 bytes` -> `506,804 bytes`
  - 淨減少 `51` 行，`2,435 bytes`
- `MatchGiftTriggerService.java`
  - `827` 行 -> `681` 行
  - `29,101 bytes` -> `23,272 bytes`
  - 淨減少 `146` 行，`5,829 bytes`
- `MatchActionService.java`
  - 維持 `10,256` 行，`408,416 bytes`

### 判讀

- 這一步主要是在補齊 Gift trigger 內部模組邊界，而不是直接再砍大量 facade
- `MatchEffectService` 再少掉一段 Gift 專屬 payload/context 組裝
- `MatchGiftTriggerService` 也開始從「大而全入口」往「協調 service + runtime context helper」的方向收斂

### 驗證結果

- `MatchGiftTriggerContextService`、`MatchGiftTriggerService`、`MatchEffectService` 的局部 `javac` 編譯可通過
- `./mvnw -q -DskipTests compile` 仍未作為準線，因 repo 內既有 `LobbyMatchService` / DTO / entity 編譯問題尚未解決
- 本步未執行 integration test

### 下一步

- 盤點 `MatchEffectService` 內剩餘的 Gift 專屬 execution callback
- 評估是否把 Gift selection preview 組裝再往外移一段
- 如果 Gift 線已足夠穩定，就開始切 `MatchActionService.resolveDecision(...)`

---

## Step A2-1

日期：2026-04-24
狀態：已完成
主題：抽出 resolveDecision 的 Gift confirm 聚合服務

### 本步完成內容

- 新增 `MatchTriggeredGiftResolutionService`
- 將 `MatchActionService` 內 gift confirm 後的逐筆觸發與聚合邏輯抽出：
  - `applyGiftTriggeredEffectsFromContext(...)`
  - `isHbp01124StoredTrigger(...)`
  - `applyHbp01124StoredTriggerEffect(...)`
- `applyGiftTriggeredEffectsAfterConfirm(...)`
- `applyCollabPostTriggeredEffectsAfterConfirm(...)`
- `applyAttackArtPostTriggeredEffectsAfterConfirm(...)`
  改為委派 `MatchTriggeredGiftResolutionService` 處理 Gift 觸發部分

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTriggeredGiftResolutionService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `10,256` 行 -> `9,996` 行
  - `408,416 bytes` -> `396,621 bytes`
  - 淨減少 `260` 行，`11,795 bytes`
- `MatchTriggeredGiftResolutionService.java`
  - 新增 `311` 行，`13,794 bytes`
- `MatchEffectService.java`
  - 維持 `12,582` 行，`506,804 bytes`

### 判讀

- 這是 `MatchActionService.resolveDecision(...)` 拆分的第一個實質步驟
- 拆出去的不是單純工具，而是 trigger confirm 之後的 Gift resolution 協調邏輯
- `MatchActionService` 已經從五位數行數降到 `9,996` 行，第一次明顯跌破 `10k`

### 驗證結果

- `MatchTriggeredGiftResolutionService`、`MatchGiftTriggerService`、`MatchEffectService` 的局部 `javac` 編譯可通過
- `./mvnw -q -DskipTests compile` 仍未作為準線，因 repo 內既有 `LobbyMatchService` / DTO / entity 編譯問題尚未解決
- 本步未執行 integration test

### 下一步

- 繼續拆 `resolveDecision(...)` 裡的 trigger confirm 分支
- 優先考慮把 `applyCollabPostTriggeredEffectsAfterConfirm(...)` / `applyAttackArtPostTriggeredEffectsAfterConfirm(...)` 再往 decision resolution service 收斂
- 等 trigger confirm 區塊邊界穩定後，再切 `TURN_START / DRAW_REVEAL / SEND_CHEER` 這類 interaction resolution

---

## Step A2-2

日期：2026-04-24
狀態：已完成
主題：抽出 trigger confirm 的一般後處理協調 service

### 本步完成內容

- 新增 `MatchTriggeredEffectResolutionService`
- 將下列 trigger confirm 後處理從 `MatchActionService` 搬到新 service：
  - `applyCollabPostTriggeredEffectsAfterConfirm(...)`
  - `applyAttackArtPostTriggeredEffectsAfterConfirm(...)`
  - `applyEffectPostTriggeredEffectsAfterConfirm(...)`
- 一併收斂其共用的 summary 聚合邏輯
- `MatchActionService` 目前對這三段只保留薄 wrapper 與 pending context 解包

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTriggeredEffectResolutionService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,996` 行 -> `9,814` 行
  - `396,621 bytes` -> `387,958 bytes`
  - 淨減少 `182` 行，`8,663 bytes`
- `MatchTriggeredEffectResolutionService.java`
  - 新增 `251` 行，`10,628 bytes`
- `MatchEffectService.java`
  - 維持 `12,582` 行，`506,804 bytes`

### 判讀

- 這一步把 `resolveDecision(...)` 內 trigger confirm 分支的第二層協調抽掉
- 現在 `MatchActionService` 內 Gift confirm、Collab confirm、Attack Art post trigger confirm 都開始有獨立的 resolution service
- `MatchActionService` 已從最初的 `10,248` 行降到 `9,814` 行，結構上開始真正從 God Class 退下來

### 驗證結果

- `MatchTriggeredEffectResolutionService`、`MatchTriggeredGiftResolutionService`、`MatchGiftTriggerService`、`MatchEffectService` 的局部 `javac` 編譯可通過
- 以 `MatchActionService` 為準的整體 `javac` / Maven compile 仍不適合作為準線，原因是 repo 內既有 DTO / entity / lobby 相關編譯缺口
- 本步未執行 integration test

### 下一步

- 繼續縮 `resolveDecision(...)` 主方法本身
- 優先切 `INTERACTION_TYPE_TRIGGER_EFFECT_CONFIRM` 這整個分支的主流程協調
- 若該段已足夠穩，再進一步拆 `TURN_START / DRAW_REVEAL / SEND_CHEER` interaction resolution

---

## Step A2-3

日期：2026-04-24
狀態：已完成
主題：抽出 `TRIGGER_EFFECT_CONFIRM` 主流程 helper

### 本步完成內容

- 將 `resolveDecision(...)` 內 `INTERACTION_TYPE_TRIGGER_EFFECT_CONFIRM` 分支收成獨立 helper：
  - `resolveTriggerEffectConfirmDecision(...)`
  - `applyConfirmedTriggerEffectResolution(...)`
- 將下列責任從 `resolveDecision(...)` 主方法本體抽離：
  - confirm payload 建立
  - 選牌驗證
  - trigger effect 執行
  - followup interaction 建立
  - defeat / cheer enqueue 後處理
- 保持既有行為與 service 邊界不變，這一步只做主流程協調整理，不動規則判斷

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,814` 行 -> `9,835` 行
  - `387,958 bytes` -> `388,208 bytes`
  - 淨增加 `21` 行，`250 bytes`
- `MatchEffectService.java`
  - 維持 `12,582` 行，`506,804 bytes`
- `MatchTriggeredGiftResolutionService.java`
  - 維持 `311` 行，`13,794 bytes`
- `MatchTriggeredEffectResolutionService.java`
  - 維持 `251` 行，`10,628 bytes`

### 判讀

- 這一步不是瘦身 step，而是主方法降噪 step
- `MatchActionService` 體積小幅回升，但 `resolveDecision(...)` 的 trigger confirm 主分支已不再直接承載完整細節
- 接下來若要再往外抽 service，會比之前更容易切出完整的 interaction resolution 模組

### 驗證結果

- `git diff --check` 通過
- gift / trigger 相關 service 的局部 `javac` 編譯可通過
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續拆 `resolveDecision(...)` 的 interaction resolution 主流程
- 優先考慮將 `TURN_START`、`DRAW_REVEAL`、`SEND_CHEER` 各自收成獨立 helper
- 若 trigger confirm 邊界已夠穩，再評估是否值得抽成正式的 `DecisionResolutionService`

---

## Step A2-4

日期：2026-04-24
狀態：已完成
主題：抽出 interaction resolution helper

### 本步完成內容

- 將 `resolveDecision(...)` 內下列 interaction 分支抽成獨立 helper：
  - `resolveTurnStartDecision(...)`
  - `resolveLiveStartDecision(...)`
  - `resolveDrawRevealDecision(...)`
  - `resolveSendCheerDecision(...)`
- `resolveDecision(...)` 主方法現在在這幾段只保留分派
- 本步不調整規則判斷，只做 interaction lifecycle 主流程收斂

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,835` 行 -> `9,874` 行
  - `388,208 bytes` -> `388,220 bytes`
  - 淨增加 `39` 行，`12 bytes`
- `MatchEffectService.java`
  - 維持 `12,582` 行，`506,804 bytes`
- `MatchGiftTriggerService.java`
  - 維持 `681` 行，`23,272 bytes`

### 判讀

- 這一步同樣屬於主流程降噪，不是瘦身 step
- `MatchActionService` 體積幾乎不變，但 `resolveDecision(...)` 內 interaction 分支的可讀性已經比前一版清楚很多
- 下一階段如果要抽正式的 `DecisionResolutionService`，現在已有較穩定的 helper 邊界可用

### 驗證結果

- 先做一次快速檢查：
  - `git diff --check` 通過
  - gift / trigger 相關 service 的局部 `javac` 編譯可通過
- 本步完成後：
  - `git diff --check` 再次通過
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續縮 `resolveDecision(...)` 主方法
- 優先盤點 `SUPPORT_DECISION_TYPE_CARD_SELECTION` 與 `LOOK_* / REORDER_*` 這段是否要抽 helper
- 若 interaction 分支已足夠穩定，再評估抽正式 `DecisionResolutionService`

---

## Step A2-5

日期：2026-04-24
狀態：已完成
主題：抽出 `LOOK / REORDER / CARD_SELECTION` decision helper

### 本步完成內容

- 將 `resolveDecision(...)` 後半段下列 decision 分支抽成獨立 helper：
  - `resolveLookTopDeckDecision(...)`
  - `resolveLookZoneDecision(...)`
  - `resolveReorderDeckBottomDecision(...)`
  - `resolveSupportCardSelectionDecision(...)`
- `resolveDecision(...)` 現在在這些分支只保留型別分派
- 本步不調整效果規則與 followup 規則，只做主流程收斂

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,874` 行 -> `9,912` 行
  - `388,220 bytes` -> `389,006 bytes`
  - 淨增加 `38` 行，`786 bytes`
- `MatchEffectService.java`
  - 維持 `12,582` 行，`506,804 bytes`
- `MatchGiftTriggerService.java`
  - 維持 `681` 行，`23,272 bytes`

### 判讀

- 這一步仍是主流程降噪，不是瘦身 step
- `MatchActionService` 尺寸小幅上升，但 `resolveDecision(...)` 的後半段 decision 處理已經比原本更接近可獨立模組化的結構
- 如果後面要抽 `DecisionResolutionService`，目前 helper 分段已經開始成形

### 驗證結果

- 先做一次快速檢查：
  - `git diff --check` 通過
  - gift / trigger 相關 service 的局部 `javac` 編譯可通過
- 本步完成後：
  - `git diff --check` 再次通過
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續盤點 `resolveDecision(...)` 是否還有值得抽出的共用後處理
- 評估 `resolveDecision(...)` 是否已適合收成正式 `DecisionResolutionService`
- 若 action 主流程邊界已穩，再回頭看 `MatchEffectService` 的下一個 effect family 拆分點

---

## Step A2-6

日期：2026-04-24
狀態：已完成
主題：抽出 action 共用 effect 後處理 helper

### 本步完成內容

- 新增 `finalizeResolvedEffect(...)`
- 將下列重複的 action 後處理收成單點：
  - match finish 檢查
  - life defeat 檢查
  - no-holomem defeat 檢查
  - life loss send cheer enqueue
- 目前已套用到：
  - `playSupport(...)`
  - `applyConfirmedTriggerEffectResolution(...)`
  - `resolveSupportCardSelectionDecision(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,912` 行 -> `9,901` 行
  - `389,006 bytes` -> `387,879 bytes`
  - 淨減少 `11` 行，`1,127 bytes`
- `MatchEffectService.java`
  - 維持 `12,582` 行，`506,804 bytes`
- `MatchGiftTriggerService.java`
  - 維持 `681` 行，`23,272 bytes`

### 判讀

- 這一步是低風險去重 step
- 雖然幅度不大，但 action 側的共用後處理現在已經有單一入口，後面若要再抽 `DecisionResolutionService` 會更容易
- `MatchActionService` 也因此從 `9,912` 行回落到 `9,901` 行

### 驗證結果

- `git diff --check` 通過
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 盤點 `resolveDecision(...)` 是否還剩值得抽離的共用 interaction helper
- 評估 action 主流程是否已經適合抽出正式 `DecisionResolutionService`
- 若 action 線暫時收斂，就回到 `MatchEffectService` 繼續切下一個 effect family

---

## Step E3-1

日期：2026-04-24
狀態：已完成
主題：抽出 damage/down 公開入口 facade

### 本步完成內容

- 新增 `MatchEffectDamageService`
- 將下列公開入口從 `MatchEffectService` 搬到新 service：
  - `applyArtDamage(...)`
  - `previewDownEventEffect(...)`
  - `applyDownEventEffect(...)`
- `MatchEffectService` 保留底層執行能力：
  - `executeDamageEffect(...)`
  - `executeDownEvent(...)`
- 更新呼叫端：
  - `MatchActionService`
  - `MatchTriggeredEffectResolutionService`
  - `OfficialCardSmokeCoverageIntegrationTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchEffectDamageService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/main/java/com/hololive/cardgame/service/MatchTriggeredEffectResolutionService.java`

### 體積變化

- `MatchEffectService.java`
  - `12,582` 行 -> `12,497` 行
  - `506,804 bytes` -> `504,335 bytes`
  - 淨減少 `85` 行，`2,469 bytes`
- `MatchEffectDamageService.java`
  - 新增 `103` 行，`2,904 bytes`
- `MatchActionService.java`
  - `9,901` 行 -> `9,910` 行
  - `387,879 bytes` -> `388,197 bytes`
  - 淨增加 `9` 行，`318 bytes`
- `MatchTriggeredEffectResolutionService.java`
  - `251` 行 -> `254` 行
  - `10,628 bytes` -> `10,834 bytes`
  - 淨增加 `3` 行，`206 bytes`

### 判讀

- 這一步是低風險的 effect facade 拆分
- `MatchEffectService` 雖然只瘦一小段，但已經開始把 `damage/down` 這類公開入口交給獨立 service
- 這比直接搬動 `executeDamageEffect(...)` / `executeDownEvent(...)` 的內部規則安全很多，也替後續 effect family 模組化留出邊界

### 驗證結果

- `git diff --check` 通過
- 以 `MatchEffectDamageService`、`MatchTriggeredEffectResolutionService`、`MatchEffectService` 為主的局部 `javac` 編譯可通過
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續盤點 `MatchEffectService` 還有哪些公開 facade 適合先搬到 family service
- 優先考慮 `LOOK / SEARCH` 或 `DAMAGE / DOWN` 周邊是否有下一層 facade 可拆
- 若 effect facade 邊界夠穩，再評估是否進一步下刀到底層執行器

---

## Step E3-2

日期：2026-04-24
狀態：已完成
主題：抽出 bloom/collab preview 與 extra-bloom allowance facade

### 本步完成內容

- 新增 `MatchTriggeredCardEffectService`
- 將下列公開入口從 `MatchEffectService` 搬到新 service：
  - `applyPassiveGiftExtraBloomAllowanceOnBloom(...)`
  - `previewBloomTriggeredEffect(...)`
  - `previewCollabTriggeredEffect(...)`
- `MatchEffectService` 保留底層 plan / runtime context / execution helper
- 更新呼叫端：
  - `MatchActionService`
  - `OfficialCardSmokeCoverageIntegrationTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTriggeredCardEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchEffectService.java`
  - `12,497` 行 -> `12,386` 行
  - `504,335 bytes` -> `499,883 bytes`
  - 淨減少 `111` 行，`4,452 bytes`
- `MatchTriggeredCardEffectService.java`
  - 新增 `107` 行，`4,076 bytes`
- `MatchActionService.java`
  - `9,910` 行 -> `9,913` 行
  - `388,197 bytes` -> `388,472 bytes`
  - 淨增加 `3` 行，`275 bytes`

### 判讀

- 這一步仍屬於低風險 facade 拆分，但比上一刀更有結構收益
- `MatchEffectService` 現在不再同時扛 `damage/down` 與 `bloom/collab preview` 這兩組公開入口
- 雖然底層執行規則仍在主檔內，但公開責任邊界已開始成形

### 驗證結果

- `git diff --check` 通過
- 以 `MatchTriggeredCardEffectService`、`MatchEffectService`、`MatchEffectDamageService` 為主的局部 `javac` 編譯可通過
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續盤點 `MatchEffectService` 還有哪些公開 facade 適合先搬到 family service
- 若 effect facade 邊界已足夠穩，再評估是否開始拆 bloom/collab 的 apply 執行邏輯
- 也可視情況回頭做一次 action/effect 的階段性正式 review，確認這批拆分已適合進入 commit 節點

---

## Step E3-3

日期：2026-04-24
狀態：已完成
主題：抽出 combat modifier facade

### 本步完成內容

- 新增 `MatchEffectCombatModifierService`
- 將下列公開入口從 `MatchEffectService` 搬到新 service：
  - `resolveAttachedSupportHpBonus(...)`
  - `resolveAttachedSupportArtBonus(...)`
  - `resolveAttachedSupportIncomingDamageReduction(...)`
  - `previewAttachedSupportConditionalTriggers(...)`
  - `resolvePassiveGiftArtBonus(...)`
  - `resolvePassiveGiftArtCheerCostReduction(...)`
  - `resolveArtTextDamageBonus(...)`
  - `resolvePassiveGiftIncomingDamageReduction(...)`
  - `resolvePassiveGiftHpBonus(...)`
- `MatchEffectService` 保留底層 helper / loader / matcher，並調整必要的 package-private 可見性，供新 service 沿用
- 更新呼叫端：
  - `MatchActionService`
  - `MatchGameStateService`
  - `OfficialCardSmokeCoverageIntegrationTest`
  - `MatchActionServiceIntegrationTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchEffectCombatModifierService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/main/java/com/hololive/cardgame/service/MatchGameStateService.java`
- `src/test/java/com/hololive/cardgame/service/MatchIntegrationTestSupport.java`
- `src/test/java/com/hololive/cardgame/service/OfficialCardSmokeCoverageIntegrationTest.java`
- `src/test/java/com/hololive/cardgame/service/MatchActionServiceIntegrationTest.java`

### 體積變化

- `MatchEffectService.java`
  - `12,386` 行 -> `12,054` 行
  - `499,883 bytes` -> `485,323 bytes`
  - 淨減少 `332` 行，`14,560 bytes`
- `MatchEffectCombatModifierService.java`
  - 新增 `323` 行，`12,707 bytes`
- `MatchActionService.java`
  - `9,913` 行 -> `9,917` 行
  - `388,472 bytes` -> `388,810 bytes`
  - 淨增加 `4` 行，`338 bytes`

### 判讀

- 這一步是目前 `MatchEffectService` facade 拆分裡最完整的一刀
- 相比前兩步只搬單一領域入口，這次抽出的已經是一整個「戰鬥修正 family」
- `MatchEffectService` 不再同時對外承擔附加 support 靜態加成、被動 Gift 攻防修正、以及條件觸發 preview 這幾組入口，責任邊界更清楚
- 底層規則尚未離開主檔，但公開 API 已經成功收斂到獨立 service，後續若要繼續往下切內部 helper，風險會比直接在 God Class 上動刀低很多

### 驗證結果

- `git diff --check` 通過
- `MatchEffectCombatModifierService` 與 `MatchEffectService` 的局部 `javac` 編譯可通過
- `MatchActionService` / `MatchGameStateService` 目前仍無法拿整體 `javac` 或 Maven compile 當準線，原因仍是 repo 既有的 DTO / entity getter-setter 缺口，不是這一步新引入的錯誤
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 盤點 `MatchEffectService` 剩餘公開入口中，是否還有下一組可整批抽出的 family service
- 優先候選：
  - `applyArtDownTriggeredEffects(...)`
  - `resolveTriggeredGiftDamagePrevention(...)`
  - `clearExpiredTurnEffects(...)` 所屬的 turn-effect lifecycle
- 若 effect facade 邊界已經夠穩，也可以回頭補一次階段性正式 review，再決定是否進入下一個 commit 節點

---

## Step E3-4

日期：2026-04-24
狀態：已完成
主題：抽出 triggered combat facade

### 本步完成內容

- 新增 `MatchTriggeredCombatEffectService`
- 將下列戰鬥中觸發反應入口從 `MatchEffectService` 對外移出：
  - `resolveTriggeredGiftDamagePrevention(...)`
  - `applyArtDownTriggeredEffects(...)`
- `MatchEffectService` 將上述兩個方法降為 package-private，改由新 service 對外提供 facade
- 更新呼叫端：
  - `MatchActionService`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTriggeredCombatEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchEffectService.java`
  - `12,054` 行 -> `12,054` 行
  - `485,323 bytes` -> `485,309 bytes`
  - 淨減少 `0` 行，`14 bytes`
- `MatchTriggeredCombatEffectService.java`
  - 新增 `48` 行，`1,314 bytes`
- `MatchActionService.java`
  - `9,917` 行 -> `9,920` 行
  - `388,810 bytes` -> `389,088 bytes`
  - 淨增加 `3` 行，`278 bytes`

### 判讀

- 這一步是低風險的 reaction facade 拆分
- 雖然 `MatchEffectService` 體積幾乎沒有變化，但公開責任再少一組，`MatchActionService` 現在不再直接依賴 `MatchEffectService` 處理這兩個 combat trigger
- 這讓後續若要繼續往下切 `damage prevention` 或 `art down` 的內部 helper，可以先在獨立 service 邊界上做，而不是從 God Class 直接動刀

### 驗證結果

- `git diff --check` 通過
- `MatchTriggeredCombatEffectService` 與 `MatchEffectService` 的局部 `javac` 編譯可通過
- `MatchActionService` 仍無法拿整體 `javac` 或 Maven compile 當準線，原因仍是 repo 既有的 DTO / entity getter-setter 缺口，不是這一步新引入的錯誤
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 盤點 `MatchEffectService` 剩餘公開入口中，是否要先拆 `turn-effect lifecycle`
- 優先候選：
  - `clearExpiredTurnEffects(...)`
  - `applyBloomTriggeredEffects(...)`
  - `applyCollabTriggeredEffects(...)`
- 若要保持低風險節奏，建議先切 `turn-effect lifecycle`；若要追求更明顯瘦身，則優先切 `bloom/collab apply`

---

## Step E3-5

日期：2026-04-24
狀態：已完成
主題：抽出 turn-effect maintenance facade

### 本步完成內容

- 新增 `MatchTurnEffectMaintenanceService`
- 將 `clearExpiredTurnEffects(...)` 從 `MatchEffectService` 對外搬出，改由新 service 提供 facade
- `MatchEffectService` 將該方法降為 package-private
- 更新呼叫端：
  - `MatchActionService.endTurn(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnEffectMaintenanceService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchEffectService.java`
  - `12,054` 行 -> `12,054` 行
  - `485,309 bytes` -> `485,302 bytes`
  - 淨減少 `0` 行，`7 bytes`
- `MatchTurnEffectMaintenanceService.java`
  - 新增 `17` 行，`513 bytes`
- `MatchActionService.java`
  - `9,920` 行 -> `9,923` 行
  - `389,088 bytes` -> `389,351 bytes`
  - 淨增加 `3` 行，`263 bytes`

### 判讀

- 這一步非常小，但方向是對的：`MatchActionService` 又少一個直接打到 `MatchEffectService` 的 turn-lifecycle 入口
- 目前還只是 lifecycle 的第一個切口，真正有價值的是後續把 turn-effect 的建立 / 清理 / 查詢再一起收攏
- 單看體積縮減幾乎沒有感覺，但結構上已經為 turn-effect family 留出獨立邊界

### 驗證結果

- `git diff --check` 通過
- `MatchTurnEffectMaintenanceService` 與 `MatchEffectService` 的局部 `javac` 編譯可通過
- `MatchActionService` 仍無法拿整體 `javac` 或 Maven compile 當準線，原因仍是 repo 既有的 DTO / entity getter-setter 缺口，不是這一步新引入的錯誤
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 若要保持低風險節奏，可以繼續補齊 `turn-effect lifecycle` 的其他入口
- 若要追求更明顯的主檔瘦身，下一刀應改切：
  - `applyBloomTriggeredEffects(...)`
  - `applyCollabTriggeredEffects(...)`
- 兩者相比，我建議先切 `bloom/collab apply`，因為已經有 preview / extra-bloom facade 當支點，延續性最好

---

## Step E3-6

日期：2026-04-24
狀態：已完成
主題：將 bloom/collab apply 入口收進 triggered card effect service

### 本步完成內容

- 擴充 `MatchTriggeredCardEffectService`
- 將下列對外入口集中到既有的 triggered card effect family：
  - `applyBloomTriggeredEffects(...)`
  - `applyCollabTriggeredEffects(...)`
- `MatchEffectService` 內對應方法維持底層執行角色，並降為 package-private
- 更新呼叫端：
  - `MatchActionService`
  - `MatchTriggeredEffectResolutionService`
  - `OfficialCardSmokeCoverageIntegrationTest`
  - `MatchActionServiceIntegrationTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTriggeredCardEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/main/java/com/hololive/cardgame/service/MatchTriggeredEffectResolutionService.java`
- `src/test/java/com/hololive/cardgame/service/OfficialCardSmokeCoverageIntegrationTest.java`
- `src/test/java/com/hololive/cardgame/service/MatchActionServiceIntegrationTest.java`

### 體積變化

- `MatchEffectService.java`
  - `12,054` 行 -> `12,054` 行
  - `485,302 bytes` -> `485,281 bytes`
  - 淨減少 `0` 行，`21 bytes`
- `MatchTriggeredCardEffectService.java`
  - `107` 行 -> `141` 行
  - `4,076 bytes` -> `5,108 bytes`
  - 淨增加 `34` 行，`1,032 bytes`
- `MatchTriggeredEffectResolutionService.java`
  - `254` 行 -> `257` 行
  - `10,834 bytes` -> `11,083 bytes`
  - 淨增加 `3` 行，`249 bytes`
- `MatchActionService.java`
  - `9,923` 行 -> `9,923` 行
  - `389,351 bytes` -> `389,364 bytes`
  - 淨增加 `0` 行，`13 bytes`

### 判讀

- 這一步沒有明顯縮小 `MatchEffectService` 體積，但把 `bloom/collab` 的 preview + apply 對外入口正式收斂到同一個 family service
- 結構收益比體積收益大很多：
  - `MatchActionService` 不再直接碰 bloom apply
  - `MatchTriggeredEffectResolutionService` 也不再直接打進 `MatchEffectService`
- 這代表 `bloom/collab` 這條線的公開邊界已經基本成形，後續若要繼續往內部 execution helper 下刀，風險會比直接在主檔上改 switch 小

### 驗證結果

- `git diff --check` 通過
- `MatchTriggeredCardEffectService`、`MatchTriggeredEffectResolutionService`、`MatchEffectService` 的局部 `javac` 編譯可通過
- `MatchActionService` / 整體 Maven compile 仍不能當準線，原因仍是 repo 既有的 DTO / entity getter-setter 缺口，不是這一步新引入的錯誤
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 若要繼續 effect facade 線，可以開始盤點 `applySupportEffect(...)` / `buildSupportDecisionPlan(...)` 是否適合拆成 support family
- 若要回頭拉低 `MatchActionService` 風險，則可以改切 `advancePhase / endTurn / drawTurn` 這條 lifecycle
- 兩者相比，我建議先做一次階段性的正式 review，再決定下一個 commit 節點

---

## Step A3-1

日期：2026-04-24
狀態：已完成
主題：抽出 end turn lifecycle handoff service

### 本步完成內容

- 新增 `MatchTurnLifecycleService`
- 將 `endTurn(...)` 內的回合交棒、狀態推進、`TURN_START` pending 建立、以及 `END_TURN / INTERACTION_PENDING` action 寫入搬到新 service
- `MatchActionService.endTurn(...)` 現在只保留：
  - `ActionContext` 載入
  - phase / pending gate
  - 缺少回合必要動作檢查
  - `clearExpiredTurnEffects / resetRested / centerReplenish` 結果準備
  - 委派 `matchTurnLifecycleService.completeEndTurn(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnLifecycleService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,923` 行 -> `9,879` 行
  - `389,351 bytes` -> `387,590 bytes`
  - 淨減少 `44` 行，`1,761 bytes`
- `MatchTurnLifecycleService.java`
  - 新增 `189` 行，`6,691 bytes`

### 判讀

- 這一步是 `MatchActionService` lifecycle 拆分的第一個有效切口
- 雖然 `resetRestedHolomemsForTurnStart(...)` 與 `resolveEndTurnCenterReplenishCycle(...)` 還留在主檔，但 `endTurn(...)` 最肥的 handoff orchestration 已經不再直接堆在主方法裡
- 下一步若要繼續切 lifecycle，可以沿同一條線把：
  - `resetRestedHolomemsForTurnStart(...)`
  - `resolveEndTurnCenterReplenishCycle(...)`
  - `createTurnStartPendingInteraction(...)`
  再逐步往 `MatchTurnLifecycleService` 收

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `MatchActionService.endTurn(...)` 改走 `matchTurnLifecycleService.completeEndTurn(...)`
  - `MatchTurnLifecycleService` 已接手 turn handoff 主流程
- 這一步的局部 `javac` 無法作為乾淨準線：
  - 新 service 直接依賴 `MatchEntity` / `MatchActionEntity`
  - 在目前 sandbox 的裸 `javac` 路徑下，會碰到 repo 既有的 entity getter-setter / annotation processing 缺口
  - 這不是這一步新引入的語法錯誤類型，而是目前環境對這個 repo 的既有限制
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續 `MatchActionService` 的 lifecycle 拆分，優先把 `endTurn(...)` 仍留在主檔的：
  - `resetRestedHolomemsForTurnStart(...)`
  - `resolveEndTurnCenterReplenishCycle(...)`
  搬進 `MatchTurnLifecycleService`
- 若要換另一條線，也可以開始拆 `advancePhase(...)` 的 `MAIN -> PERFORMANCE -> END` phase transition orchestration

---

## Step A3-2

日期：2026-04-24
狀態：已完成
主題：將 end turn 的重置與補位流程搬進 turn lifecycle service

### 本步完成內容

- 擴充 `MatchTurnLifecycleService`
- 將 `endTurn(...)` 內原本仍留在 `MatchActionService` 的兩段搬出：
  - `resetRestedHolomemsForTurnStart(...)`
  - `resolveEndTurnCenterReplenishCycle(...)`
- 同步將只服務於這條線的 helper 一併搬走：
  - `autoReplenishCenterFromBackOnce(...)`
  - `hasCenterHolomem(...)`
- `MatchActionService.endTurn(...)` 現在只剩：
  - context / pending / phase gate
  - 缺少回合必要動作檢查
  - 委派 turn effect maintenance
  - 委派 turn lifecycle service

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnLifecycleService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,879` 行 -> `9,681` 行
  - `387,590 bytes` -> `380,714 bytes`
  - 淨減少 `198` 行，`6,876 bytes`
- `MatchTurnLifecycleService.java`
  - `189` 行 -> `504` 行
  - `6,691 bytes` -> `17,306 bytes`
  - 淨增加 `315` 行，`10,615 bytes`

### 判讀

- 這一步之後，`endTurn(...)` 才算真正有被抽薄
- `MatchActionService` 不再自己處理：
  - 回合開始重置休息狀態
  - END_TURN 前的 CENTER 自動補位循環
- 這讓 `MatchTurnLifecycleService` 已經不只是單純 handoff facade，而是開始承擔真正的回合收束邏輯
- 目前仍留在 `MatchActionService` 的 lifecycle 主要重量，下一個就是 `advancePhase(...)`

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `MatchActionService.endTurn(...)` 改走 `matchTurnLifecycleService.resetRestedHolomemsForTurnStart(...)`
  - `MatchActionService.endTurn(...)` 改走 `matchTurnLifecycleService.resolveEndTurnCenterReplenishCycle(...)`
- 這一步仍未以裸 `javac` 拿到乾淨準線：
  - `MatchTurnLifecycleService` 直接依賴 `MatchEntity` / `MatchActionEntity`
  - 在目前 sandbox 的裸 `javac` 路徑下，仍會碰到 repo 既有的 entity getter-setter / annotation processing 缺口
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 直接開始拆 `advancePhase(...)` 的 lifecycle orchestration
- 優先切出：
  - phase 推進決策
  - `MAIN -> PERFORMANCE` 的 performance start gift trigger 流程
  - `PERFORMANCE -> END` 的 performance end gift trigger 流程
- 若要保持低風險，可以先只抽 phase 推進與 gift trigger pending 建立，不碰底層 gift 規則

---

## Step A3-3

日期：2026-04-24
狀態：已完成
主題：將 advance phase 的推進與 gift follow-up 協調抽成 helper

### 本步完成內容

- 收斂 `advancePhase(...)` 主方法：
  - 抽出 `validateRequiredTurnActionsCompleted(...)`
  - 抽出 `resolveNextAdvancePhase(...)`
  - 抽出 `prepareAdvancePhaseFollowup(...)`
  - 抽出 `buildAdvancePhasePayload(...)`
- 將 `MAIN -> PERFORMANCE` 與 `PERFORMANCE -> END` 的 gift preview / pending interaction 建立邏輯，分別收進：
  - `preparePerformanceStartAdvancePhaseFollowup(...)`
  - `preparePerformanceEndAdvancePhaseFollowup(...)`
- 抽出 `createDeferredGiftTriggerDecision(...)`
- 新增 `AdvancePhaseFollowup` 結構，讓 phase transition 的 follow-up 狀態不再散在主方法裡

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,681` 行 -> `9,730` 行
  - `380,714 bytes` -> `380,923 bytes`
  - 淨增加 `49` 行，`209 bytes`

### 判讀

- 這一步的主收益是 lifecycle 主流程降噪，不是檔案體積縮減
- `advancePhase(...)` 現在已經不再直接塞滿：
  - 必要回合動作檢查
  - phase 推進決策
  - performance start / end 的 gift preview 與 pending 建立
  - payload 組裝
- 雖然 `MatchActionService` 體積小幅增加，但 phase transition 的邏輯邊界已經比較清楚，後續要再搬到 service 時阻力會小很多

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `advancePhase(...)` 改走 `prepareAdvancePhaseFollowup(...)`
  - performance start / end gift 流程已收進獨立 helper
- `MatchActionService` 仍不適合用裸 `javac` 當乾淨準線：
  - repo 目前仍依賴既有 entity getter-setter / annotation processing 路徑
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 沿同一條 lifecycle 線，繼續收斂：
  - `drawTurn(...)`
  - `sendTurnCheer(...)`
- 先把 phase 切換、pending interaction 建立、action payload 組裝從入口方法本體搬開

---

## Step A3-4

日期：2026-04-24
狀態：已完成
主題：將 draw turn / send cheer 收斂成薄入口與 helper

### 本步完成內容

- 收斂 `drawTurn(...)` 主方法：
  - 抽出 `validateDrawTurnAvailable(...)`
  - 抽出 `executeDrawTurn(...)`
  - 抽出 `appendDrawTurnAction(...)`
  - 抽出 `appendDrawRevealPendingAction(...)`
  - 新增 `DrawTurnResult` 區分正常抽牌與 deck-out
- 收斂 `sendTurnCheer(...)` 主方法：
  - 抽出 `validateTurnCheerAvailable(...)`
  - 抽出 `prepareTurnCheerInteraction(...)`
  - 抽出 `appendTurnCheerPendingAction(...)`
- 新增 `moveMatchPhase(...)`，集中 phase 切換與 `updatedAt` / flush 行為

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,730` 行 -> `9,791` 行
  - `380,923 bytes` -> `383,023 bytes`
  - 淨增加 `61` 行，`2,100 bytes`

### 判讀

- 這一步同樣不是為了立刻瘦身，而是把 lifecycle 入口變成真正的薄 wrapper
- `drawTurn(...)` 與 `sendTurnCheer(...)` 現在都只剩：
  - context / pending gate
  - 基本可用性驗證
  - 委派 helper 完成 phase 切換與 pending action 組裝
- 這會讓下一刀若要把 lifecycle 入口正式搬到 `MatchTurnLifecycleService`，不必再先拆一輪方法內部分支

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `drawTurn(...)` 改走 `executeDrawTurn(...)`
  - `drawTurn(...)` 改走 `moveMatchPhase(...)`
  - `sendTurnCheer(...)` 改走 `prepareTurnCheerInteraction(...)`
  - `sendTurnCheer(...)` 改走 `appendTurnCheerPendingAction(...)`
- `MatchActionService` 仍不適合用裸 `javac` 當乾淨準線：
  - repo 目前仍依賴既有 entity getter-setter / annotation processing 路徑
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 判斷 lifecycle 邊界是否已足夠穩定，進一步選擇其一：
  - 將 `drawTurn / sendTurnCheer / advancePhase` 正式搬進 `MatchTurnLifecycleService`
  - 繼續拆 `advanceOpeningSetup(...)`，把 opening flow 也納入 lifecycle 模組

---

## Step A3-5

日期：2026-04-24
狀態：已完成
主題：將 opening setup handoff 搬進 turn lifecycle service

### 本步完成內容

- 在 `MatchTurnLifecycleService` 新增 `completeOpeningSetup(...)`
- 將 opening setup 的主要 handoff 責任搬出 `MatchActionService`：
  - 驗證是否已放置 opening center
  - 驗證是否已完成 opening setup
  - 寫入 `OPENING_SETUP_DONE`
  - 決定下一位 opening setup 玩家
  - 全部完成時將回合控制權切回 `playerA`
- `MatchActionService.advanceOpeningSetup(...)` 現在只保留：
  - 玩家存在性檢查
  - mulligan 完成驗證
  - 當 opening setup 全部完成時補 `LIVE_START` pending interaction
- 移除 `MatchActionService` 中已不再使用的 opening setup helper：
  - `resolveNextOpeningSetupUser(...)`
  - `hasOpeningSetupFinished(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnLifecycleService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,791` 行 -> `9,738` 行
  - `383,023 bytes` -> `381,164 bytes`
  - 淨減少 `53` 行，`1,859 bytes`
- `MatchTurnLifecycleService.java`
  - `504` 行 -> `569` 行
  - `17,306 bytes` -> `19,744 bytes`
  - 淨增加 `65` 行，`2,438 bytes`

### 判讀

- 這一步之後，opening flow 已經開始真正進入 lifecycle 模組，而不是單純留在 `MatchActionService` 內做 helper 降噪
- `MatchTurnLifecycleService` 現在同時承接：
  - end turn handoff
  - turn start reset / center replenish
  - opening setup handoff
- `MatchActionService` 在 lifecycle 這條線上又少了一組完整的流程責任

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `MatchActionService.advanceOpeningSetup(...)` 改走 `matchTurnLifecycleService.completeOpeningSetup(...)`
  - `LIVE_START` pending interaction 仍由 `MatchActionService` 建立
- `MatchTurnLifecycleService` 仍不適合用裸 `javac` 當乾淨準線：
  - repo 目前仍依賴既有 entity getter-setter / annotation processing 路徑
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續沿 lifecycle 線收斂，優先選一個：
  - 把 `drawTurn / sendTurnCheer / advancePhase` 正式搬進 `MatchTurnLifecycleService`
  - 或先把 `LIVE_START` interaction 建立也從 `MatchActionService` 往外收，讓 opening flow 更完整

---

## Step A3-6

日期：2026-04-24
狀態：已完成
主題：將 LIVE_START pending interaction 也搬進 turn lifecycle service

### 本步完成內容

- 擴充 `MatchTurnLifecycleService.completeOpeningSetup(...)`
  - opening setup 全部完成時，直接建立 `LIVE_START` pending decision
  - 同步寫入 `INTERACTION_PENDING` action
- 在 `MatchTurnLifecycleService` 新增：
  - `appendLiveStartPendingInteraction(...)`
  - `createLiveStartPendingInteraction(...)`
- `MatchActionService.advanceOpeningSetup(...)` 現在只剩：
  - 查詢 opening player
  - mulligan 完成驗證
  - 委派 `matchTurnLifecycleService.completeOpeningSetup(...)`
- 從 `MatchActionService` 移除不再使用的 `createLiveStartPendingInteraction(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnLifecycleService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,738` 行 -> `9,677` 行
  - `381,164 bytes` -> `378,781 bytes`
  - 淨減少 `61` 行，`2,383 bytes`
- `MatchTurnLifecycleService.java`
  - `569` 行 -> `630` 行
  - `19,744 bytes` -> `22,148 bytes`
  - 淨增加 `61` 行，`2,404 bytes`

### 判讀

- opening flow 到這一步才算真正閉合
- `MatchActionService` 已不再自己處理 opening setup 完成後的：
  - action 寫入
  - turn handoff
  - `LIVE_START` pending 建立
- `MatchTurnLifecycleService` 現在已經承接一整段完整 lifecycle：
  - opening setup 完成
  - end turn handoff
  - turn start reset / center replenish

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `MatchActionService.advanceOpeningSetup(...)` 只剩委派 `matchTurnLifecycleService.completeOpeningSetup(...)`
  - `LIVE_START` pending interaction 已移到 `MatchTurnLifecycleService`
- `MatchTurnLifecycleService` 仍不適合用裸 `javac` 當乾淨準線：
  - repo 目前仍依賴既有 entity getter-setter / annotation processing 路徑
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 進入 lifecycle 第二階段，正式評估三個入口誰最適合整段搬進 `MatchTurnLifecycleService`：
  - `drawTurn(...)`
  - `sendTurnCheer(...)`
  - `advancePhase(...)`
- 目前我會優先選 `sendTurnCheer(...)`，因為耦合面最小、入口最短、成功率最高

---

## Step A3-7

日期：2026-04-24
狀態：已完成
主題：將 turn cheer 的 lifecycle handoff 搬進 turn lifecycle service

### 本步完成內容

- 在 `MatchTurnLifecycleService` 新增 `beginTurnCheer(...)`
- 將 `sendTurnCheer(...)` 的 lifecycle 責任搬出 `MatchActionService`：
  - 切換到 `CHEER` phase
  - 寫入 `INTERACTION_PENDING` action
- `MatchActionService.sendTurnCheer(...)` 現在只剩：
  - context / pending gate
  - turn cheer 可用性驗證
  - 建立 turn cheer pending interaction
  - 委派 `matchTurnLifecycleService.beginTurnCheer(...)`
- 移除 `MatchActionService` 中不再使用的 `appendTurnCheerPendingAction(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnLifecycleService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,677` 行 -> `9,662` 行
  - `378,781 bytes` -> `378,151 bytes`
  - 淨減少 `15` 行，`630 bytes`
- `MatchTurnLifecycleService.java`
  - `630` 行 -> `653` 行
  - `22,148 bytes` -> `23,213 bytes`
  - 淨增加 `23` 行，`1,065 bytes`

### 判讀

- 這一步雖然不大，但代表 lifecycle 第二階段已開始
- `sendTurnCheer(...)` 已經不再自己處理 phase 切換與 pending action 寫入
- 這是把 `drawTurn(...)` / `advancePhase(...)` 往 lifecycle service 進一步搬移前，最穩的一個切口

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `MatchActionService.sendTurnCheer(...)` 改走 `matchTurnLifecycleService.beginTurnCheer(...)`
  - `appendTurnCheerPendingAction(...)` 已從 `MatchActionService` 移除
- `MatchTurnLifecycleService` 仍不適合用裸 `javac` 當乾淨準線：
  - repo 目前仍依賴既有 entity getter-setter / annotation processing 路徑
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 繼續 lifecycle 第二階段，優先處理 `drawTurn(...)`
- 若要維持低風險，先只搬：
  - 抽牌後 phase 切換
  - `DRAW_REVEAL` pending action 寫入
- `drawTopDeckCardToHand(...)` 與 deck-out 判斷可暫時留在 `MatchActionService`

---

## Step A3-8

日期：2026-04-24
狀態：已完成
主題：將 draw turn 的 lifecycle handoff 搬進 turn lifecycle service

### 本步完成內容

- 在 `MatchTurnLifecycleService` 新增 `beginDrawTurn(...)`
- 將 `drawTurn(...)` 的 lifecycle 責任搬出 `MatchActionService`：
  - 切換到 `DRAW` phase
  - 寫入 `DRAW_REVEAL` 的 `INTERACTION_PENDING` action
- `MatchActionService.drawTurn(...)` 現在只剩：
  - context / pending gate
  - draw turn 可用性驗證
  - 實際抽牌與 deck-out 判斷
  - `DRAW_TURN` action 寫入
  - 委派 `matchTurnLifecycleService.beginDrawTurn(...)`
- 移除 `MatchActionService` 中不再使用的：
  - `appendDrawRevealPendingAction(...)`
  - `moveMatchPhase(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnLifecycleService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,662` 行 -> `9,631` 行
  - `378,151 bytes` -> `377,067 bytes`
  - 淨減少 `31` 行，`1,084 bytes`
- `MatchTurnLifecycleService.java`
  - `653` 行 -> `686` 行
  - `23,213 bytes` -> `24,540 bytes`
  - 淨增加 `33` 行，`1,327 bytes`

### 判讀

- 這一步之後，`drawTurn(...)` 與 `sendTurnCheer(...)` 都已不再自己處理 phase 切換與 pending action 寫入
- lifecycle 第二階段目前已經先收掉兩個最穩的入口
- 剩下最重、也最值得下一步處理的，就是 `advancePhase(...)`

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `MatchActionService.drawTurn(...)` 改走 `matchTurnLifecycleService.beginDrawTurn(...)`
  - `appendDrawRevealPendingAction(...)` 已從 `MatchActionService` 移除
  - `moveMatchPhase(...)` 已從 `MatchActionService` 移除
- `MatchTurnLifecycleService` 仍不適合用裸 `javac` 當乾淨準線：
  - repo 目前仍依賴既有 entity getter-setter / annotation processing 路徑
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 進入 lifecycle 第二階段的主體，開始處理 `advancePhase(...)`
- 若要保持低風險，先只搬：
  - phase 切換與 match flush
  - `ADVANCE_PHASE` action 寫入
  - performance start / end follow-up payload 組裝暫時仍留在 `MatchActionService`

---

## Step A3-9

日期：2026-04-24
狀態：已完成
主題：將 advance phase 的 lifecycle handoff 搬進 turn lifecycle service

### 本步完成內容

- 在 `MatchTurnLifecycleService` 新增 `advancePhase(...)`
- 將 `advancePhase(...)` 的 lifecycle 責任搬出 `MatchActionService`：
  - phase 切換
  - `updatedAt` / flush
  - `ADVANCE_PHASE` action 寫入
- `MatchActionService.advancePhase(...)` 現在只剩：
  - context / pending gate
  - phase 驗證與 next phase 推導
  - performance start / end 的 gift follow-up 準備
  - payload 組裝
  - 委派 `matchTurnLifecycleService.advancePhase(...)`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchTurnLifecycleService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`

### 體積變化

- `MatchActionService.java`
  - `9,631` 行 -> `9,627` 行
  - `377,067 bytes` -> `376,929 bytes`
  - 淨減少 `4` 行，`138 bytes`
- `MatchTurnLifecycleService.java`
  - `686` 行 -> `710` 行
  - `24,540 bytes` -> `25,310 bytes`
  - 淨增加 `24` 行，`770 bytes`

### 判讀

- 這一步之後，`advancePhase(...)` 雖然核心規則仍留在 `MatchActionService`，但 lifecycle handoff 已經先切出去
- 到目前為止，lifecycle 第二階段已經完成：
  - `sendTurnCheer(...)` handoff
  - `drawTurn(...)` handoff
  - `advancePhase(...)` handoff
- `MatchTurnLifecycleService` 現在已經成為真正的回合流程寫回模組，而不只是 end turn 專用 service

### 驗證結果

- `git diff --check` 通過
- 結構檢查已確認：
  - `MatchActionService.advancePhase(...)` 改走 `matchTurnLifecycleService.advancePhase(...)`
  - `ADVANCE_PHASE` action 寫入已移到 `MatchTurnLifecycleService`
- `MatchTurnLifecycleService` 仍不適合用裸 `javac` 當乾淨準線：
  - repo 目前仍依賴既有 entity getter-setter / annotation processing 路徑
- integration test 仍無法在目前 sandbox 下作為準線：
  - Testcontainers 無法連 Docker socket
  - Flyway / PostgreSQL 連線因 `Operation not permitted` 失敗

### 下一步

- 判斷是否進入下一個可 commit 節點
- 若繼續拆，最自然的方向是：
  - 將 `advancePhase(...)` 中 performance start / end 的 gift follow-up 準備，再切成更明確的 transition helper 或 service
  - 或回頭補一次階段性正式 review，確認 lifecycle 第二階段是否已收斂到可提交狀態

---

## Step B1-1

日期：2026-04-27
狀態：已完成
主題：啟動 BLOOM 第二條 pilot contract 包與第一刀 bridge

### 本步完成內容

- 依 `16-BLOOM 第二條 Pilot 啟動規劃.md` 補齊 BLOOM contract 包：
  - `17-BLOOM Action Contract.md`
  - `18-BLOOM Validator Contract.md`
  - `19-BLOOM Resolution Contract.md`
  - `20-BLOOM Trigger Contract.md`
  - `21-BLOOM Persistence Contract.md`
  - `22-BLOOM Pilot Acceptance.md`
- 建立 BLOOM bridge 第一版 production skeleton：
  - `BloomAction`
  - `BloomValidationContext`
  - `BloomActionValidator`
  - `BloomActionResolver`
  - `BloomApplicationService`
  - `BloomLegacyResolutionBridge`
  - event / trigger contract classes
- 將 `MatchActionService.bloom(...)` 改為 adapter/facade：
  - 由舊入口建構 `BloomAction`
  - validation 改走 `BloomApplicationService.validate(...)`
  - state mutation 改走 `BloomApplicationService.resolveState(...)`
  - legacy effect preview / pending confirm / action log 暫留為 compatibility glue
- 補 focused tests：
  - `BloomActionValidatorTest`
  - `BloomActionResolverTest`
  - `BloomEventFactoryTest`
  - `BloomApplicationServiceTest`
  - `BloomApplicationServiceIntegrationTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/main/java/com/hololive/cardgame/service/Bloom*.java`
- `src/test/java/com/hololive/cardgame/service/Bloom*.java`
- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/17-BLOOM Action Contract.md`
- `doc/系統優化/18-BLOOM Validator Contract.md`
- `doc/系統優化/19-BLOOM Resolution Contract.md`
- `doc/系統優化/20-BLOOM Trigger Contract.md`
- `doc/系統優化/21-BLOOM Persistence Contract.md`
- `doc/系統優化/22-BLOOM Pilot Acceptance.md`

### 體積變化

- `MatchActionService.java`
  - `9,627` 行 -> `9,426` 行
  - `376,929 bytes` -> `368,670 bytes`
  - 淨減少 `201` 行，`8,259 bytes`
- BLOOM contract docs
  - 新增 `1,047` 行，`23,649 bytes`
- BLOOM production skeleton
  - `BloomApplicationService.java`：`54` 行，`2,280 bytes`
  - `BloomActionValidator.java`：`113` 行，`5,846 bytes`
  - `BloomActionResolver.java`：`170` 行，`5,832 bytes`

### 判讀

- BLOOM 已開始形成第二條真實 use case，而不是只新增空泛 interface
- 目前符合 `16` 文件中的階段 1、階段 2，並已先放入階段 3 的 event / trigger path skeleton
- preview / confirm 的實際 persistence 仍暫留 legacy glue，這是 `22-BLOOM Pilot Acceptance.md` 允許的暫時技術債

### 驗證結果

- 通過：
  - `git diff --check`
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=BloomActionValidatorTest,BloomActionResolverTest,BloomEventFactoryTest,BloomApplicationServiceTest,BloomApplicationServiceIntegrationTest test`
  - `./mvnw -q -Dtest=MatchActionServiceIntegrationTest#bloomShouldRejectSkippingLevelTransition+bloomShouldRejectTargetEnteredThisTurn test`
- 第一次未 escalated 執行 integration test 時，Testcontainers 被 sandbox 擋住 Docker socket，重跑授權後通過
- 曾觀察到既有 `bloomShouldUpgradeHolomemAndKeepStack` 受 legacy effect confirm 流程影響而失敗；該現象在 `MatchActionService` 無本次 diff 時也可重現，因此不列為本 BLOOM adapter blocker

### 下一步

- 依 `22-BLOOM Pilot Acceptance.md` 做 BLOOM 階段性 review：
  - 檢查 `MatchActionService.bloom(...)` 是否只剩 adapter 與 compatibility glue
  - 補 `git diff --check` / compile 驗證
  - 判斷是否需要把 preview / confirm persistence 從 legacy glue 再往 `BloomEffectPreviewHandler` / `BloomTriggerConfirmHandler` 收斂

---

## Step B1-2

日期：2026-04-27
狀態：已完成
主題：收斂 BLOOM effect preview / confirm compatibility glue

### 本步完成內容

- 新增 `BloomEffectResolutionService`，承接 BLOOM state resolve 後的 effect follow-up 協調：
  - passive gift extra bloom allowance 套用
  - bloom triggered effect preview
  - `onHolomemBloom(...)` hook summary
  - `BLOOM_EFFECT` confirm pending decision 建立
  - trigger resolution order payload
- 新增 `BloomEffectResolution`，讓 BLOOM adapter 以結構化結果回填 action payload 與 event result
- `MatchActionService.bloom(...)` 再瘦身：
  - 不再直接呼叫 `previewBloomTriggeredEffect(...)`
  - 不再直接建立 `BLOOM_EFFECT` confirm pending interaction
  - 不再直接組 `triggerResolutionOrder`
- 補 `BloomEffectResolutionServiceTest`，固定無 effect 時不建立 pending decision，且維持既有 payload summary shape

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/main/java/com/hololive/cardgame/service/BloomEffectResolution.java`
- `src/main/java/com/hololive/cardgame/service/BloomEffectResolutionService.java`
- `src/test/java/com/hololive/cardgame/service/BloomEffectResolutionServiceTest.java`
- `doc/系統優化/05-重構進度追蹤.md`

### 體積變化

- `MatchActionService.java`
  - `9,426` 行 -> `9,393` 行
  - `368,670 bytes` -> `367,425 bytes`
  - 淨減少 `33` 行，`1,245 bytes`
- 新增 `BloomEffectResolutionService.java`
  - `316` 行，`11,741 bytes`
- 新增 `BloomEffectResolution.java`
  - `22` 行，`766 bytes`

### 判讀

- BLOOM Phase 3 的 preview / confirm path 已不再直接塞在 `MatchActionService.bloom(...)` 主方法裡
- 目前 pending decision 寫入仍是 legacy table contract，但 responsibility 已集中到 BLOOM 專用 service
- 下一步若要更乾淨，可把 `BloomEffectResolutionService` 內的 pending decision persistence 再拆成通用 follow-up interaction writer；這屬於跨 use case 抽象，應等 BLOOM pilot review 後再決定

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=BloomEffectResolutionServiceTest test`
  - `./mvnw -q -Dtest=BloomActionValidatorTest,BloomActionResolverTest,BloomEventFactoryTest,BloomApplicationServiceTest,BloomEffectResolutionServiceTest test`
  - `./mvnw -q -Dtest=BloomApplicationServiceIntegrationTest test`
  - `./mvnw -q -Dtest=MatchActionServiceIntegrationTest#bloomShouldRejectSkippingLevelTransition+bloomShouldRejectTargetEnteredThisTurn test`
  - `git diff --check`

### 下一步

- 進入 BLOOM pilot acceptance review：
  - 對照 `22-BLOOM Pilot Acceptance.md`
  - 確認哪些 legacy helper 可留到下一輪
  - 判斷是否到下一個 commit 節點

---

## Step B1-3

日期：2026-04-27
狀態：已完成
主題：完成 BLOOM pilot acceptance review

### 本步完成內容

- 新增 `23-BLOOM Pilot Acceptance Review.md`
- 對照 `22-BLOOM Pilot Acceptance.md` 逐項確認：
  - 新流程已存在
  - 舊入口已橋接
  - validation / mutation / effect follow-up / event trigger 已分層
  - focused tests 已覆蓋主要 contract
  - legacy glue 邊界可清楚說明
- 從 `MatchActionService` 移除已不被呼叫的舊 BLOOM helper：
  - `findExtraBloomAllowanceId(...)`
  - `consumeExtraBloomAllowance(...)`
  - `loadOwnedBloomTarget(...)`
  - `isSpecialOrUnbloomableLevel(...)`
  - `isBloomLevelNextStep(...)`
  - `canIgnoreBloomLevelByPassiveGift(...)`
  - `resolveBloomLevelRank(...)`
  - 舊 `BloomTarget` record
- 更新 `00-系統優化總覽.md`，將 BLOOM acceptance review 納入文件入口

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/05-重構進度追蹤.md`
- `doc/系統優化/23-BLOOM Pilot Acceptance Review.md`

### 判讀

- BLOOM pilot 已達到第二條可複製 use case 的階段性完成標準
- 舊 bloom 主方法內不再保留未使用的 validation / mutation helper
- 接下來不建議直接進 `ATTACK`；第三條 pilot 建議從 `COLLAB` 或 `ATTACH_CHEER` 選一條

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=BloomActionValidatorTest,BloomActionResolverTest,BloomEventFactoryTest,BloomApplicationServiceTest,BloomEffectResolutionServiceTest test`

### 下一步

- 進入第三條 pilot 規劃，建議優先補：
  - `COLLAB Pilot 啟動規劃`
  - 或 `Card Action Pilot 共性整理`

---

## Step C1-1

日期：2026-04-27
狀態：已完成
主題：啟動 COLLAB 第三條 pilot 規劃

### 本步完成內容

- 新增 `24-COLLAB 第三條 Pilot 啟動規劃.md`
- 確認第三條 pilot 優先選 `COLLAB`，而不是直接進 `ATTACK`
- 盤點目前 `MatchActionService.moveStageHolomem(... targetZone=COLLAB)` 的責任混雜：
  - BACK -> COLLAB movement
  - top deck -> holopower
  - collab effect preview
  - gift trigger preview
  - confirm pending interaction
  - action payload / action log
- 定義 `COLLAB` pilot 的四階段路線：
  - 文件定義
  - 第一刀 bridge
  - effect follow-up path
  - 驗收 review
- 更新 `00-系統優化總覽.md`

### 主要影響檔案

- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/05-重構進度追蹤.md`
- `doc/系統優化/24-COLLAB 第三條 Pilot 啟動規劃.md`

### 判讀

- `COLLAB` 是比 `ATTACK` 更合理的第三條 pilot：
  - 它有 effect preview / confirm
  - 它碰 gift trigger preview
  - 它仍比 battle core 小
- 下一步應先補 `COLLAB` contract 包，不直接改 production code

### 驗證結果

- 本步為文件規劃，尚未改 production code

### 下一步

- 補 `COLLAB` contract 包：
  - `COLLAB Action Contract`
  - `COLLAB Validator Contract`
  - `COLLAB Resolution Contract`
  - `COLLAB Effect Follow-up Contract`
  - `COLLAB Event / Trigger Contract`
  - `COLLAB Persistence Contract`
  - `COLLAB Pilot Acceptance`

---

## Step C1-2

日期：2026-04-27
狀態：已完成
主題：補齊 COLLAB contract 包

### 本步完成內容

- 依 `24-COLLAB 第三條 Pilot 啟動規劃.md` 補齊 COLLAB contract 包：
  - `25-COLLAB Action Contract.md`
  - `26-COLLAB Validator Contract.md`
  - `27-COLLAB Resolution Contract.md`
  - `28-COLLAB Effect Follow-up Contract.md`
  - `29-COLLAB Event Trigger Contract.md`
  - `30-COLLAB Persistence Contract.md`
  - `31-COLLAB Pilot Acceptance.md`
- 明確固定第三條 pilot 的責任邊界：
  - `MoveStageHolomemActionRequest.targetZone = COLLAB` 是 legacy input
  - 新流程應轉成 `CollabAction`
  - `MOVE_STAGE_HOLOMEM` 的非-COLLAB 分支暫不搬
- 更新 `00-系統優化總覽.md`

### 主要影響檔案

- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/05-重構進度追蹤.md`
- `doc/系統優化/25-COLLAB Action Contract.md`
- `doc/系統優化/26-COLLAB Validator Contract.md`
- `doc/系統優化/27-COLLAB Resolution Contract.md`
- `doc/系統優化/28-COLLAB Effect Follow-up Contract.md`
- `doc/系統優化/29-COLLAB Event Trigger Contract.md`
- `doc/系統優化/30-COLLAB Persistence Contract.md`
- `doc/系統優化/31-COLLAB Pilot Acceptance.md`

### 判讀

- COLLAB pilot 已具備可施工的文件基準
- 下一步可開始第一刀 bridge
- 第一刀應只處理 `targetZone = COLLAB`，不要順手搬一般 CENTER movement

### 驗證結果

- 本步為文件規劃，尚未改 production code

### 下一步

- 開始 COLLAB 第一刀 bridge：
  - 建立 `CollabAction / CollabValidationContext / CollabValidationResult`
  - 建立 `CollabActionValidator`
  - 建立 `CollabLegacyResolutionBridge`
  - 建立 `CollabActionResolver`
  - 建立 `CollabApplicationService`
  - 先讓 `MatchActionService.moveStageHolomem(... targetZone=COLLAB)` 可委派新流程

---

## Step C1-3

日期：2026-04-27
狀態：已完成
主題：完成 COLLAB 第一刀 bridge

### 本步完成內容

- 新增 COLLAB 第一刀 production contract：
  - `CollabAction`
  - `CollabSourceHolomemSnapshot`
  - `CollabValidationContext`
  - `CollabValidationResult`
  - `CollabResolutionResult`
  - `CollabActionValidator`
  - `CollabLegacyResolutionBridge`
  - `CollabActionResolver`
  - `CollabApplicationService`
- `MatchActionService.moveStageHolomem(... targetZone=COLLAB)` 改為：
  - legacy DTO / target zone routing
  - 轉成 `CollabAction`
  - 委派 `CollabApplicationService.validate(...)`
  - 委派 `CollabApplicationService.resolveState(...)`
  - 保留既有 collab effect / gift preview / confirm / action log 相容 glue
- 一般 `targetZone=CENTER` movement 暫時維持 legacy branch，不和 COLLAB 第一刀混改。
- 新增 focused unit tests：
  - `CollabActionValidatorTest`
  - `CollabActionResolverTest`
  - `CollabApplicationServiceTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/CollabAction.java`
- `src/main/java/com/hololive/cardgame/service/CollabSourceHolomemSnapshot.java`
- `src/main/java/com/hololive/cardgame/service/CollabValidationContext.java`
- `src/main/java/com/hololive/cardgame/service/CollabValidationResult.java`
- `src/main/java/com/hololive/cardgame/service/CollabResolutionResult.java`
- `src/main/java/com/hololive/cardgame/service/CollabActionValidator.java`
- `src/main/java/com/hololive/cardgame/service/CollabLegacyResolutionBridge.java`
- `src/main/java/com/hololive/cardgame/service/CollabActionResolver.java`
- `src/main/java/com/hololive/cardgame/service/CollabApplicationService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/test/java/com/hololive/cardgame/service/CollabActionValidatorTest.java`
- `src/test/java/com/hololive/cardgame/service/CollabActionResolverTest.java`
- `src/test/java/com/hololive/cardgame/service/CollabApplicationServiceTest.java`

### 體積變化

- `MatchActionService.java`
  - `9,190` 行 -> `9,306` 行
  - `361,654 bytes` -> `366,329 bytes`
  - 淨增加 `116` 行，`4,675 bytes`
- 新增 COLLAB production contract / bridge：
  - `CollabAction.java`：`57` 行，`1,445 bytes`
  - `CollabActionValidator.java`：`74` 行，`3,704 bytes`
  - `CollabActionResolver.java`：`106` 行，`3,390 bytes`
  - `CollabApplicationService.java`：`42` 行，`1,675 bytes`
  - `CollabLegacyResolutionBridge.java`：`289` 行，`9,687 bytes`

### 判讀

- COLLAB 的 validation 與 state mutation 已離開 `MatchActionService` 主流程。
- 目前保留在 `MatchActionService` 的是 effect follow-up / confirm / payload 相容 glue，符合第一刀 bridge 範圍。
- 下一步應抽 `CollabEffectResolutionService`，再把 preview / confirm 建立從舊方法移出。

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=CollabActionValidatorTest,CollabActionResolverTest,CollabApplicationServiceTest test`
  - `./mvnw -q -Dtest=MatchActionServiceIntegrationTest#collabShouldIncludeResolutionOrderWithPriority+collabShouldCreateGiftConfirmWhenGiftTriggeredByOwnHolomemCollab test`
- 第一次 integration smoke 在 sandbox 內被 Docker/Testcontainers socket 權限擋下；同一指令以 escalated Docker 權限重跑後通過。

### 下一步

- 進入 COLLAB 第二刀 effect follow-up path：
  - 新增 `CollabEffectResolutionService`
  - 將 collab triggered effect preview / gift preview / event hook / confirm pending interaction 建立移出 `MatchActionService`
  - 補 `CollabEffectResolutionServiceTest`
  - 保持 action log payload 與現有 resolve decision path 相容

---

## Step C1-4

日期：2026-04-27
狀態：已完成
主題：抽出 COLLAB effect follow-up path

### 本步完成內容

- 新增 `CollabEffectResolution`
- 新增 `CollabEffectResolutionService`
- 將 COLLAB 專屬 follow-up orchestration 從 `MatchActionService` 移出：
  - collab triggered effect preview
  - gift trigger preview
  - collab event hook
  - confirm pending interaction 建立
  - collab / gift trigger sections
  - `triggerResolutionOrder`
- `MatchActionService.executeCollabAction(...)` 現在只保留：
  - 呼叫 `CollabApplicationService`
  - 呼叫 `CollabEffectResolutionService`
  - 組 legacy action payload
  - append action log
  - 暫時保留既有非 deferred 勝負檢查與 life-loss send cheer follow-up
- 移除 `MatchActionService` 中已不用的 COLLAB 專屬 private helper。
- 補 `CollabEffectResolutionServiceTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/CollabEffectResolution.java`
- `src/main/java/com/hololive/cardgame/service/CollabEffectResolutionService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/test/java/com/hololive/cardgame/service/CollabEffectResolutionServiceTest.java`
- `doc/系統優化/05-重構進度追蹤.md`

### 體積變化

- `MatchActionService.java`
  - `9,306` 行 -> `9,059` 行
  - `366,329 bytes` -> `355,644 bytes`
  - 淨減少 `247` 行，`10,685 bytes`
- 新增 COLLAB effect follow-up：
  - `CollabEffectResolution.java`：`42` 行，`1,531 bytes`
  - `CollabEffectResolutionService.java`：`683` 行，`28,013 bytes`

### 判讀

- COLLAB 的 validation、state mutation、effect preview / confirm 建立都已離開 `MatchActionService`。
- `MatchActionService` 仍保留 action payload compatibility 與勝負檢查 glue，符合 `31-COLLAB Pilot Acceptance.md` 允許暫存項。
- 下一步應補 COLLAB event / trigger contract 的 production 化，而不是直接進 ATTACK。

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=CollabActionValidatorTest,CollabActionResolverTest,CollabApplicationServiceTest,CollabEffectResolutionServiceTest test`
  - `./mvnw -q -Dtest=MatchActionServiceIntegrationTest#collabShouldIncludeResolutionOrderWithPriority+collabShouldCreateGiftConfirmWhenGiftTriggeredByOwnHolomemCollab test`

### 下一步

- 進入 COLLAB 第三刀 event / trigger path：
  - 新增 `CollabEvent`
  - 新增 `CollabEventType`
  - 新增 `CollabEventFactory`
  - 先建立 event factory 與 ordering 測試
  - 暫不抽共用泛型 dispatcher

---

## Step C1-5

日期：2026-04-27
狀態：已完成
主題：落地 COLLAB event / trigger path

### 本步完成內容

- 新增 COLLAB event contract 的 production 型別：
  - `CollabEventType`
  - `CollabEvent`
  - `CollabEventFactory`
- 新增 COLLAB trigger dispatch contract：
  - `CollabTriggerExecutionMode`
  - `CollabTriggerHandlingResult`
  - `CollabTriggerHandler`
  - `CollabTriggerDispatchResult`
  - `CollabTriggerDispatcher`
- 新增 thin handler 分類：
  - `CollabSystemStateFinalizationHandler`
  - `CollabEffectPreviewHandler`
  - `CollabGiftPreviewHandler`
  - `CollabTriggerConfirmHandler`
- `CollabApplicationService` 新增 `dispatchResolvedEvents(...)`
- `MatchActionService.executeCollabAction(...)` 在 effect resolution 後 dispatch COLLAB events
- 補 `CollabEventFactoryTest`，驗證 event ordering 與 sync / deferred handler 分類

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/CollabEventType.java`
- `src/main/java/com/hololive/cardgame/service/CollabEvent.java`
- `src/main/java/com/hololive/cardgame/service/CollabEventFactory.java`
- `src/main/java/com/hololive/cardgame/service/CollabTriggerExecutionMode.java`
- `src/main/java/com/hololive/cardgame/service/CollabTriggerHandlingResult.java`
- `src/main/java/com/hololive/cardgame/service/CollabTriggerHandler.java`
- `src/main/java/com/hololive/cardgame/service/CollabTriggerDispatchResult.java`
- `src/main/java/com/hololive/cardgame/service/CollabTriggerDispatcher.java`
- `src/main/java/com/hololive/cardgame/service/CollabSystemStateFinalizationHandler.java`
- `src/main/java/com/hololive/cardgame/service/CollabEffectPreviewHandler.java`
- `src/main/java/com/hololive/cardgame/service/CollabGiftPreviewHandler.java`
- `src/main/java/com/hololive/cardgame/service/CollabTriggerConfirmHandler.java`
- `src/main/java/com/hololive/cardgame/service/CollabApplicationService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/test/java/com/hololive/cardgame/service/CollabEventFactoryTest.java`
- `src/test/java/com/hololive/cardgame/service/CollabApplicationServiceTest.java`

### 體積變化

- `MatchActionService.java`
  - `9,059` 行 -> `9,060` 行
  - `355,644 bytes` -> `355,745 bytes`
  - 淨增加 `1` 行，`101 bytes`
- 新增 COLLAB event / trigger 基礎檔：
  - `CollabEvent.java`：`18` 行，`380 bytes`
  - `CollabEventFactory.java`：`130` 行，`4,550 bytes`
  - `CollabTriggerDispatcher.java`：`64` 行，`2,462 bytes`
  - `CollabTriggerHandlingResult.java`：`24` 行，`857 bytes`

### 判讀

- COLLAB 已具備 Action / Validator / Resolver / EffectResolution / Event / Trigger / Application 的完整 pilot path。
- 目前 handler 仍是 thin/no-op，主要用途是固定事件順序與 sync/deferred 邊界。
- 不抽共用泛型 dispatcher，避免在第三條 pilot 尚未驗收前提前做框架化。

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=CollabActionValidatorTest,CollabActionResolverTest,CollabApplicationServiceTest,CollabEffectResolutionServiceTest,CollabEventFactoryTest test`
  - `./mvnw -q -Dtest=MatchActionServiceIntegrationTest#collabShouldIncludeResolutionOrderWithPriority+collabShouldCreateGiftConfirmWhenGiftTriggeredByOwnHolomemCollab test`

### 下一步

- 進入 COLLAB pilot acceptance review：
  - 對照 `31-COLLAB Pilot Acceptance.md`
  - 檢查舊入口 allow / block 清單
  - 補必要的 review 文件或缺口修正
  - 若無 blocker，準備 commit

---

## Step C1-6

日期：2026-04-27
狀態：已完成
主題：完成 COLLAB pilot acceptance review

### 本步完成內容

- 對照 `31-COLLAB Pilot Acceptance.md` 檢查 COLLAB 完成條件。
- 補 `32-COLLAB Pilot Acceptance Review.md`，記錄：
  - 架構條件對照
  - 舊入口 allow / block 清單
  - 不可退步契約對照
  - focused unit / integration 驗證結果
  - 可接受暫留技術債
  - 下一條 pilot 建議
- 新增 `CollabApplicationServiceIntegrationTest`，補齊 direct application `resolveState` DB mutation 測試缺口：
  - BACK Holomem -> COLLAB
  - top deck card -> HOLOPOWER
  - `CollabResolutionResult.holopowerCardInstanceId` 與 DB mutation 一致
- 更新 `00-系統優化總覽.md` 文件索引。

### 主要影響檔案

- `src/test/java/com/hololive/cardgame/service/CollabApplicationServiceIntegrationTest.java`
- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/05-重構進度追蹤.md`
- `doc/系統優化/32-COLLAB Pilot Acceptance Review.md`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,060` 行，`355,745 bytes`
- `MatchEffectService.java`
  - 維持 `12,054` 行，`485,281 bytes`
- 新增 acceptance integration test：
  - `CollabApplicationServiceIntegrationTest.java`：`160` 行，`6,108 bytes`

### 判讀

- COLLAB pilot 已達到第三條可複製 use case 的階段性驗收標準。
- 本步沒有再擴 production 行為，只補測試與驗收文件。
- direct application integration test 已補齊 `31` 文件要求的測試缺口，因此目前沒有 acceptance blocker。

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=CollabActionValidatorTest,CollabActionResolverTest,CollabApplicationServiceTest,CollabEffectResolutionServiceTest,CollabEventFactoryTest test`
  - `./mvnw -q -Dtest=CollabApplicationServiceIntegrationTest test`

### 下一步

- 準備 commit：完成 COLLAB pilot 驗收 review。
- commit 後建議進入第四條 pilot 規劃，優先評估 `ATTACH_CHEER`。

---

## Step C2-1

日期：2026-04-27
狀態：已完成
主題：建立 ATTACH_CHEER 第四條 pilot contract 包

### 本步完成內容

- 新增 `33-ATTACH_CHEER 第四條 Pilot 啟動規劃.md`
  - 確認第四條 pilot 聚焦 resource attachment action
  - 明確排除 `SEND_CHEER`、attack cost payment、card effect 自動附 Cheer
- 新增 ATTACH_CHEER contract 文件：
  - `34-ATTACH_CHEER Action Contract.md`
  - `35-ATTACH_CHEER Validator Contract.md`
  - `36-ATTACH_CHEER Resolution Contract.md`
  - `37-ATTACH_CHEER Persistence Contract.md`
  - `38-ATTACH_CHEER Pilot Acceptance.md`
- 盤點既有 `MatchActionService.attachCheer(...)` 邊界：
  - request parsing
  - source cheer validation
  - target holomem validation
  - `match_cards` mutation
  - `match_holomem_cheers` insert
  - action log
- 更新 `00-系統優化總覽.md` 文件索引。

### 主要影響檔案

- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/05-重構進度追蹤.md`
- `doc/系統優化/33-ATTACH_CHEER 第四條 Pilot 啟動規劃.md`
- `doc/系統優化/34-ATTACH_CHEER Action Contract.md`
- `doc/系統優化/35-ATTACH_CHEER Validator Contract.md`
- `doc/系統優化/36-ATTACH_CHEER Resolution Contract.md`
- `doc/系統優化/37-ATTACH_CHEER Persistence Contract.md`
- `doc/系統優化/38-ATTACH_CHEER Pilot Acceptance.md`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,060` 行，`355,745 bytes`
- `MatchEffectService.java`
  - 維持 `12,054` 行，`485,281 bytes`

### 判讀

- 本步只建立第四條 pilot 的施工邊界，尚未修改 production code。
- ATTACH_CHEER 明確定位為 resource attachment action，而不是 turn cheer lifecycle 或 attack cost payment。
- 下一步可開始第一刀 implementation：`AttachCheerAction` / validation context / validator。

### 驗證結果

- 通過：
  - `git diff --check`

### 下一步

- 進入 ATTACH_CHEER 第一刀 contract implementation：
  - `AttachCheerAction`
  - `AttachCheerSourceCardSnapshot`
  - `AttachCheerTargetHolomemSnapshot`
  - `AttachCheerValidationContext`
  - `AttachCheerValidationResult`
  - `AttachCheerActionValidator`
  - `AttachCheerActionValidatorTest`

---

## Step C2-2

日期：2026-04-27
狀態：已完成
主題：落地 ATTACH_CHEER action / validator contract

### 本步完成內容

- 新增 ATTACH_CHEER action contract production 型別：
  - `AttachCheerAction`
  - `AttachCheerSourceCardSnapshot`
  - `AttachCheerTargetHolomemSnapshot`
  - `AttachCheerValidationContext`
  - `AttachCheerValidationResult`
- 新增 `AttachCheerActionValidator`
- 新增 `GameErrorCode.ATTACH_CHEER_INVALID_TARGET`
- 補 `AttachCheerActionValidatorTest`，覆蓋：
  - stale action 優先順序
  - source zone 不合法
  - source 不是 Cheer
  - target Holomem 缺失
  - HAND Cheer -> own Holomem allow path

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/error/GameErrorCode.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerAction.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerSourceCardSnapshot.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerTargetHolomemSnapshot.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerValidationContext.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerValidationResult.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerActionValidator.java`
- `src/test/java/com/hololive/cardgame/service/AttachCheerActionValidatorTest.java`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,060` 行，`355,745 bytes`
- `MatchEffectService.java`
  - 維持 `12,054` 行，`485,281 bytes`
- 新增 ATTACH_CHEER contract / validator：
  - `AttachCheerAction.java`：`58` 行，`1,537 bytes`
  - `AttachCheerActionValidator.java`：`104` 行，`4,419 bytes`
  - `AttachCheerActionValidatorTest.java`：`174` 行，`5,385 bytes`

### 判讀

- ATTACH_CHEER 的主要合法性判斷已可離開 `MatchActionService.attachCheer(...)`。
- 目前尚未接 legacy bridge / resolver / application，因此 public API 行為尚未改動。
- 下一步應補 DB context loader 與 resolver，先用 direct application integration test 保護 mutation。

### 驗證結果

- 通過：
  - `./mvnw -q -Dtest=AttachCheerActionValidatorTest test`
  - `./mvnw -q -DskipTests compile`

### 下一步

- 進入 ATTACH_CHEER 第二刀 resolver / application：
  - `AttachCheerLegacyResolutionBridge`
  - `AttachCheerResolutionResult`
  - `AttachCheerActionResolver`
  - `AttachCheerApplicationService`
  - `AttachCheerActionResolverTest`
  - `AttachCheerApplicationServiceTest`
  - `AttachCheerApplicationServiceIntegrationTest`

---

## Step C2-3

日期：2026-04-27
狀態：已完成
主題：落地 ATTACH_CHEER resolver / application / adapter path

### 本步完成內容

- 新增 `AttachCheerLegacyResolutionBridge`
  - 載入 match / actor / turn / phase context
  - 載入 source Cheer card snapshot
  - 載入 target Holomem snapshot
  - 判斷 pending interaction / duplicate action / action lock
- 新增 `AttachCheerActionResolver`
  - 將 source Cheer card 從 `HAND` / `CHEER_DECK` 移到 `STAGE`
  - 將 `order_index` 清為 `NULL`
  - 將 `is_face_down` 設為 `FALSE`
  - 寫入 `match_holomem_cheers`
- 新增 `AttachCheerApplicationService`
- 新增 `AttachCheerResolutionResult`
- 補 focused tests：
  - `AttachCheerActionResolverTest`
  - `AttachCheerApplicationServiceTest`
  - `AttachCheerApplicationServiceIntegrationTest`
- `MatchActionService.attachCheer(...)` 改成 adapter：
  - 保留 request DTO 解析
  - 轉成 `AttachCheerAction`
  - 呼叫 application validate / resolve
  - 保留 `matches.current_phase = MAIN` touch
  - 保留 legacy `ATTACH_CHEER` action log payload

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/AttachCheerLegacyResolutionBridge.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerActionResolver.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerApplicationService.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerResolutionResult.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/test/java/com/hololive/cardgame/service/AttachCheerActionResolverTest.java`
- `src/test/java/com/hololive/cardgame/service/AttachCheerApplicationServiceTest.java`
- `src/test/java/com/hololive/cardgame/service/AttachCheerApplicationServiceIntegrationTest.java`

### 體積變化

- `MatchActionService.java`
  - `9,060` 行 -> `9,012` 行
  - `355,745 bytes` -> `354,442 bytes`
  - 淨減少 `48` 行，`1,303 bytes`
- 新增 ATTACH_CHEER resolver / application：
  - `AttachCheerLegacyResolutionBridge.java`：`268` 行，`9,118 bytes`
  - `AttachCheerActionResolver.java`：`83` 行，`2,747 bytes`
  - `AttachCheerApplicationService.java`：`45` 行，`1,847 bytes`
  - `AttachCheerApplicationServiceIntegrationTest.java`：`137` 行，`5,629 bytes`

### 判讀

- `MatchActionService.attachCheer(...)` 已不再承擔主要 validation 與 DB mutation。
- `ATTACH_CHEER` direct application path 已可獨立驗證 `match_cards` / `match_holomem_cheers` 一致性。
- 目前 event / trigger path 尚未落地，下一步補 thin event contract 後再進 acceptance review。

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=AttachCheerActionValidatorTest,AttachCheerActionResolverTest,AttachCheerApplicationServiceTest test`
  - `./mvnw -q -Dtest=AttachCheerApplicationServiceIntegrationTest test`
  - `./mvnw -q -Dtest=MatchActionServiceIntegrationTest#playSupportRemoveCheerShouldDetachAndArchiveCheerCard test`

### 下一步

- 進入 ATTACH_CHEER 第三刀 event / trigger path：
  - `AttachCheerEventType`
  - `AttachCheerEvent`
  - `AttachCheerEventFactory`
  - `AttachCheerTriggerDispatcher`
  - thin handler contract
  - `AttachCheerEventFactoryTest`

---

## Step C2-4

日期：2026-04-27
狀態：已完成
主題：落地 ATTACH_CHEER event / trigger path

### 本步完成內容

- 新增 ATTACH_CHEER event contract production 型別：
  - `AttachCheerEventType`
  - `AttachCheerEvent`
  - `AttachCheerEventFactory`
- 新增 ATTACH_CHEER trigger dispatch contract：
  - `AttachCheerTriggerExecutionMode`
  - `AttachCheerTriggerHandlingResult`
  - `AttachCheerTriggerHandler`
  - `AttachCheerTriggerDispatchResult`
  - `AttachCheerTriggerDispatcher`
  - `AttachCheerSystemStateFinalizationHandler`
- `AttachCheerApplicationService` 補 `dispatchResolvedEvents(...)`
- `MatchActionService.attachCheer(...)` 在 state resolution 後 dispatch resolved events
- 補 `AttachCheerEventFactoryTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/AttachCheerEventType.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerEvent.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerEventFactory.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerTriggerExecutionMode.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerTriggerHandlingResult.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerTriggerHandler.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerTriggerDispatchResult.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerTriggerDispatcher.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerSystemStateFinalizationHandler.java`
- `src/main/java/com/hololive/cardgame/service/AttachCheerApplicationService.java`
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`
- `src/test/java/com/hololive/cardgame/service/AttachCheerEventFactoryTest.java`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,012` 行，`354,442 bytes`
- 新增 ATTACH_CHEER event / trigger：
  - `AttachCheerEventFactory.java`：`44` 行，`1,584 bytes`
  - `AttachCheerTriggerDispatcher.java`：`41` 行，`1,444 bytes`
  - `AttachCheerSystemStateFinalizationHandler.java`：`20` 行，`682 bytes`
  - `AttachCheerEventFactoryTest.java`：`74` 行，`2,703 bytes`

### 判讀

- ATTACH_CHEER 已具備 Action / Validator / Resolver / Event / Trigger / Application 的完整 pilot path。
- 目前 trigger handler 只有 system state finalization thin handler，沒有引入新產品規則。
- 下一步應做 ATTACH_CHEER pilot acceptance review，再進 commit checkpoint。

### 驗證結果

- 通過：
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=AttachCheerActionValidatorTest,AttachCheerActionResolverTest,AttachCheerApplicationServiceTest,AttachCheerEventFactoryTest test`
  - `./mvnw -q -Dtest=AttachCheerApplicationServiceIntegrationTest test`
  - `./mvnw -q -Dtest=MatchActionServiceIntegrationTest#playSupportRemoveCheerShouldDetachAndArchiveCheerCard test`

### 下一步

- 進入 ATTACH_CHEER pilot acceptance review：
  - 對照 `38-ATTACH_CHEER Pilot Acceptance.md`
  - 檢查舊入口 allow/block 清單
  - 盤點測試缺口
  - 若沒有 blocker，補 acceptance review 文件並準備 commit

---

## Step C2-5

日期：2026-04-27
狀態：已完成
主題：完成 ATTACH_CHEER pilot acceptance review

### 本步完成內容

- 對照 `38-ATTACH_CHEER Pilot Acceptance.md` 檢查 ATTACH_CHEER 完成條件。
- 補 `39-ATTACH_CHEER Pilot Acceptance Review.md`，記錄：
  - 架構條件對照
  - 舊入口 allow / block 清單
  - board state / rule rejection / action log / event 契約
  - 測試缺口與暫留技術債
  - 下一條 use case 建議
- 補 `AttachCheerApplicationServiceIntegrationTest` focused rejection coverage：
  - source zone 非 `HAND` / `CHEER_DECK`
  - source card 不是 Cheer
  - target Holomem 不存在
- 更新 `00-系統優化總覽.md` 文件索引。

### 主要影響檔案

- `doc/系統優化/39-ATTACH_CHEER Pilot Acceptance Review.md`
- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/05-重構進度追蹤.md`
- `src/test/java/com/hololive/cardgame/service/AttachCheerApplicationServiceIntegrationTest.java`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,013` 行，`354,520 bytes`
- `AttachCheerApplicationServiceIntegrationTest.java`
  - `137` 行 -> `232` 行
  - `5,629 bytes` -> `9,717 bytes`
  - 淨增加 `95` 行，`4,088 bytes`

### 判讀

- ATTACH_CHEER pilot 已達到第四條可複製 use case 的階段性驗收標準。
- 舊 `MatchActionService.attachCheer(...)` 只保留 adapter / action log compatibility glue。
- `38` 文件要求的 focused integration rejection paths 已補齊，目前沒有 blocker。

### 驗證結果

- 通過：
  - `./mvnw -q -Dtest=AttachCheerApplicationServiceIntegrationTest test`

### 下一步

- 準備 commit：完成 ATTACH_CHEER pilot acceptance review。
- commit 後建議進入下一條 use case 規劃，優先評估 `PLAY_CARD`，或先做 attack cost 前置拆分；不建議直接切 `ATTACK` 主流程。

---

## Step P1-1

日期：2026-04-27
狀態：已完成
主題：建立 PLAY_CARD 第五條 pilot contract 包

### 本步完成內容

- 新增 `40-PLAY_CARD 第五條 Pilot 啟動規劃.md`
  - 確認第五條 pilot 聚焦 hand member placement action
  - 第一版範圍限定 legacy `MatchActionService.playToStage(...)`
  - 明確排除 support card、attack cost payment、attack core
- 新增 PLAY_CARD contract 文件：
  - `41-PLAY_CARD Action Contract.md`
  - `42-PLAY_CARD Validator Contract.md`
  - `43-PLAY_CARD Resolution Contract.md`
  - `44-PLAY_CARD Effect Follow-up Contract.md`
  - `45-PLAY_CARD Event Trigger Contract.md`
  - `46-PLAY_CARD Persistence Contract.md`
  - `47-PLAY_CARD Pilot Acceptance.md`
- 盤點 `playToStage(...)` 目前混雜責任：
  - RESET opening setup validation
  - MAIN hand-to-stage validation
  - `match_cards` / `match_holomems` / `match_holomem_stack_cards` mutation
  - enter hook
  - Gift preview / confirm pending decision
  - legacy action log
- 更新 `00-系統優化總覽.md` 文件索引。

### 主要影響檔案

- `doc/系統優化/40-PLAY_CARD 第五條 Pilot 啟動規劃.md`
- `doc/系統優化/41-PLAY_CARD Action Contract.md`
- `doc/系統優化/42-PLAY_CARD Validator Contract.md`
- `doc/系統優化/43-PLAY_CARD Resolution Contract.md`
- `doc/系統優化/44-PLAY_CARD Effect Follow-up Contract.md`
- `doc/系統優化/45-PLAY_CARD Event Trigger Contract.md`
- `doc/系統優化/46-PLAY_CARD Persistence Contract.md`
- `doc/系統優化/47-PLAY_CARD Pilot Acceptance.md`
- `doc/系統優化/00-系統優化總覽.md`
- `doc/系統優化/05-重構進度追蹤.md`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,013` 行，`354,520 bytes`
- 本步只建立第五條 pilot 的施工邊界，尚未修改 production code。

### 判讀

- `PLAY_CARD` 現在適合進入第五條 pilot，因為前四條已涵蓋 lifecycle、card action、stage action + follow-up、resource attachment。
- `PLAY_CARD` 第一版必須收斂為 Holomem hand-to-stage placement；support card 與 attack cost 不應混入這條 pilot。
- 因 `playToStage(...)` 包含 enter hook / Gift follow-up，這條 pilot 需要額外規劃 `Effect Follow-up Contract`。

### 驗證結果

- 通過：
  - `git diff --check`

### 下一步

- 進入 PLAY_CARD 第一刀 contract implementation：
  - `PlayCardAction`
  - `PlayCardValidationContext`
  - `PlayCardValidationResult`
  - `PlayCardActionValidator`
  - focused validator tests

---

## Step P1-2

日期：2026-04-27
狀態：已完成
主題：落地 PLAY_CARD action / validator contract

### 本步完成內容

- 新增 PLAY_CARD action contract production 型別：
  - `PlayCardAction`
  - `PlayCardSourceCardSnapshot`
  - `PlayCardValidationContext`
  - `PlayCardValidationResult`
- 新增 `PlayCardActionValidator`
  - match / lobby active gate
  - stale / duplicate gate
  - turn / phase / pending gate
  - source hand member gate
  - RESET opening setup gate
  - MAIN placement gate
  - BACK capacity gate
  - stage action lock gate
- 補 `PlayCardActionValidatorTest`
  - stale 優先順序
  - RESET DEBUT -> CENTER allow
  - RESET center 前不可先 BACK
  - RESET center 後 SPOT -> BACK allow
  - MAIN 不可放 CENTER
  - MAIN FIRST 直上拒絕並提示 BLOOM
  - source 不在 HAND
  - source 不是 MEMBER
  - BACK 已滿
  - MAIN DEBUT -> BACK allow

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/PlayCardAction.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardSourceCardSnapshot.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardValidationContext.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardValidationResult.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardActionValidator.java`
- `src/test/java/com/hololive/cardgame/service/PlayCardActionValidatorTest.java`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,013` 行，`354,520 bytes`
- 新增 PLAY_CARD contract / validator：
  - `PlayCardAction.java`：`66` 行，`1,724 bytes`
  - `PlayCardActionValidator.java`：`143` 行，`6,511 bytes`
  - `PlayCardActionValidatorTest.java`：`298` 行，`8,838 bytes`

### 判讀

- PLAY_CARD 的主要合法性判斷已可離開 `MatchActionService.playToStage(...)`。
- 目前尚未接 legacy bridge / resolver / application，因此 public API 行為尚未改動。
- 下一步應補 DB context loader 與 resolver，先用 direct application integration test 保護 `match_cards` / `match_holomems` / `match_holomem_stack_cards` mutation。

### 驗證結果

- 通過：
  - `git diff --check`
  - `./mvnw -q -Dtest=PlayCardActionValidatorTest test`
  - `./mvnw -q -DskipTests compile`

### 下一步

- 進入 PLAY_CARD 第二刀 resolver / application：
  - `PlayCardLegacyResolutionBridge`
  - `PlayCardResolutionResult`
  - `PlayCardActionResolver`
  - `PlayCardApplicationService`
  - `PlayCardActionResolverTest`
  - `PlayCardApplicationServiceTest`
  - `PlayCardApplicationServiceIntegrationTest`

---

## Step P1-3

日期：2026-04-27
狀態：已完成
主題：落地 PLAY_CARD resolver / application path

### 本步完成內容

- 新增 `PlayCardLegacyResolutionBridge`
  - 載入 match / actor / turn / phase context
  - 載入 source card snapshot 與 member metadata
  - 判斷 mulligan / opening center state
  - 判斷 pending interaction / duplicate action / action lock
  - 統計 target zone occupancy
- 新增 `PlayCardActionResolver`
  - 將 source card 從 `HAND` 移到 `STAGE`
  - 將 `order_index` 清為 `NULL`
  - 依 `openingReset` 設定 `is_face_down`
  - 寫入 `match_holomems`
  - 寫入 `match_holomem_stack_cards`
- 新增 `PlayCardApplicationService`
- 新增 `PlayCardResolutionResult`
- 補 focused tests：
  - `PlayCardActionResolverTest`
  - `PlayCardApplicationServiceTest`
  - `PlayCardApplicationServiceIntegrationTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/PlayCardLegacyResolutionBridge.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardActionResolver.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardApplicationService.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardResolutionResult.java`
- `src/test/java/com/hololive/cardgame/service/PlayCardActionResolverTest.java`
- `src/test/java/com/hololive/cardgame/service/PlayCardApplicationServiceTest.java`
- `src/test/java/com/hololive/cardgame/service/PlayCardApplicationServiceIntegrationTest.java`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,013` 行，`354,520 bytes`
- 新增 PLAY_CARD resolver / application：
  - `PlayCardLegacyResolutionBridge.java`：`275` 行，`9,261 bytes`
  - `PlayCardActionResolver.java`：`127` 行，`4,102 bytes`
  - `PlayCardApplicationService.java`：`45` 行，`1,757 bytes`
  - `PlayCardApplicationServiceIntegrationTest.java`：`154` 行，`6,438 bytes`

### 判讀

- `PLAY_CARD` direct application path 已可獨立驗證 `match_cards` / `match_holomems` / `match_holomem_stack_cards` 一致性。
- 目前尚未接 legacy `MatchActionService.playToStage(...)` adapter，也尚未抽 enter hook / Gift follow-up path。
- 下一步應補 `PlayCardEffectResolutionService`，先把 MAIN stage enter hook / Gift preview / confirm pending decision 從 adapter 之外固定 contract。

### 驗證結果

- 通過：
  - `git diff --check`
  - `./mvnw -q -Dtest=PlayCardActionValidatorTest,PlayCardActionResolverTest,PlayCardApplicationServiceTest test`
  - `./mvnw -q -DskipTests compile`
  - `./mvnw -q -Dtest=PlayCardApplicationServiceIntegrationTest test`

### 下一步

- 進入 PLAY_CARD 第三刀 effect follow-up path：
  - `PlayCardEffectResolution`
  - `PlayCardEffectResolutionService`
  - focused follow-up tests

---

## Step P1-4

日期：2026-04-27
狀態：已完成
主題：抽出 PLAY_CARD effect follow-up path

### 本步完成內容

- 新增 `PlayCardEffectResolution`
  - 固定 enter hook summary
  - 固定 Gift triggered effects / deferred summary
  - 固定 Gift confirm pending decision metadata
  - 固定 RESET deferred 狀態與 trigger resolution order
- 新增 `PlayCardEffectResolutionService`
  - RESET / opening placement 不執行 enter hook
  - RESET / opening placement 不建立 Gift preview
  - RESET / opening placement 不建立 pending confirm
  - MAIN placement 呼叫 `MatchEventHookService.onHolomemEnter(...)`
  - MAIN placement 呼叫 `MatchGiftTriggerService.previewGiftTriggeredEffectsOnStageEnter(...)`
  - MAIN placement 若 Gift triggered effects 不為空，建立 `TRIGGER_EFFECT_CONFIRM`
  - pending context 保留 source card payload / gift triggers / turn number
- 補 focused tests：
  - `PlayCardEffectResolutionServiceTest`

### 主要影響檔案

- `src/main/java/com/hololive/cardgame/service/PlayCardEffectResolution.java`
- `src/main/java/com/hololive/cardgame/service/PlayCardEffectResolutionService.java`
- `src/test/java/com/hololive/cardgame/service/PlayCardEffectResolutionServiceTest.java`

### 體積變化

- `MatchActionService.java`
  - 維持 `9,013` 行，`354,520 bytes`
- 新增 PLAY_CARD effect follow-up：
  - `PlayCardEffectResolutionService.java`：`540` 行，`20,927 bytes`
  - `PlayCardEffectResolution.java`：`32` 行，`1,153 bytes`
  - `PlayCardEffectResolutionServiceTest.java`：`174` 行，`6,713 bytes`

### 判讀

- `PLAY_CARD` 的 enter hook / Gift preview / Gift confirm pending decision 已有可獨立測試的服務邊界。
- 目前尚未將 `MatchActionService.playToStage(...)` adapter 改接新 application / effect flow。
- 下一步應進入 PLAY_CARD event / adapter 整合，讓 legacy 入口開始使用 `PlayCardApplicationService` 與 `PlayCardEffectResolutionService`，並維持既有 action log payload 相容。

### 驗證結果

- 通過：
  - `./mvnw -q -Dtest=PlayCardEffectResolutionServiceTest,PlayCardActionValidatorTest,PlayCardActionResolverTest,PlayCardApplicationServiceTest test`
  - `./mvnw -q -DskipTests compile`

### 下一步

- 進入 PLAY_CARD 第四刀 event / adapter integration：
  - `MatchActionService.playToStage(...)` 改接 `PlayCardApplicationService`
  - follow-up 改接 `PlayCardEffectResolutionService`
  - 保留 `OPENING_SET_CENTER` / `OPENING_SET_BACK` / `PLAY_TO_STAGE` action log 相容 payload
  - 補 integration regression tests

---

## 五、下一個更新規則

從這一步開始，之後每完成一個明確的優化步驟，都直接在本文件追加：

1. 新的 step 編號
2. 實際完成內容
3. 大檔尺寸變化
4. 驗證結果
5. 下一步
