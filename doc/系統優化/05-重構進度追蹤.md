# 重構進度追蹤

更新日期：2026-05-27
定位：目前重構進度摘要
完整歷史：`doc/系統優化/archive/05-重構進度追蹤-完整歷史-2026-05-25.md`

---

## 一、目前進度摘要

本輪後端優化已從「單一 God Class 內補 helper」推進到「以 effect execution family 為單位拆出 package-private service」。

目前主要成果：

- `MatchEffectService` 已從萬行級逐步降到 `5,020` 行。
- 低風險 effect family 已多數移出主 service。
- Bloom / Collab dispatcher 已逐步改成直接依賴具體 execution service，減少回頭委派 `MatchEffectService` 的 bridge。
- `MatchActionService` 目前維持 `6,099` 行，暫列第二順位。
- `MatchActionServiceIntegrationTest` 目前 `32,302` 行，暫不作為主線優先處理。

---

## 二、近期完成批次

| Step | 主題 | 結果 |
| --- | --- | --- |
| AAA-186 | Look 類效果執行服務 | 已抽出 `MatchLookEffectExecutionService` |
| AAA-187 | Draw 效果執行服務 | 已抽出 `MatchDrawEffectExecutionService` |
| AAA-188 | Holopower 移動效果執行服務 | 已抽出 `MatchHolopowerMoveEffectExecutionService` |
| AAA-189 | Rest 效果執行服務 | 已抽出 `MatchRestEffectExecutionService` |
| AAA-190 | Center / Back 交換效果執行服務 | 已抽出 `MatchSwapCenterBackEffectExecutionService` |
| AAA-191 | Collab 位置交換效果執行服務 | 已抽出 `MatchCollabSwapEffectExecutionService` |
| AAA-192 | Action Lock 效果執行服務 | 已抽出 `MatchActionLockEffectExecutionService` |
| AAA-193 | 額外 Bloom 許可效果執行服務 | 已抽出 `MatchExtraBloomAllowanceEffectExecutionService` |
| AAA-194 | Baton Touch 成本修正效果執行服務 | 已抽出 `MatchBatonTouchCostModifierEffectExecutionService` |
| AAA-195 | 勝負結果效果執行服務 | 已抽出 `MatchResultEffectExecutionService` |
| AAA-196 | 棄手牌效果執行服務 | 已抽出 `MatchDiscardHandEffectExecutionService` |
| AAA-197 | 公開歸檔效果執行服務 | 已抽出 `MatchRevealToArchiveEffectExecutionService` |
| AAA-198 | 登場效果執行服務 | 已抽出 `MatchSummonToStageEffectExecutionService` |
| AAA-199 | Archive Bloom 效果執行服務 | 已抽出 `MatchArchiveBloomEffectExecutionService` |
| AAA-200 | Cheer 回牌庫底效果執行服務 | 已抽出 `MatchCheerDeckReturnEffectExecutionService` |
| AAA-201 | Down 效果執行服務 | 已抽出 `MatchDownEffectExecutionService` |
| AAA-202 | Heal 效果執行服務 | 已抽出 `MatchHealEffectExecutionService` |
| AAA-203 | 區域移動效果執行服務 | 已抽出 `MatchMoveZoneEffectExecutionService` |
| AAA-204 | Cheer 移除效果執行服務 | 已抽出 `MatchCheerRemovalEffectExecutionService` |
| AAA-205 | Damage 重構保護測試 | 已新增 `MatchEffectDamageExecutionCharacterizationTest` |
| AAA-206 | Damage 效果執行服務 | 已抽出 `MatchDamageEffectExecutionService` |
| AAA-207 | Damage 有效 HP 快照解析 | 已抽出 `MatchDamageEffectiveHpResolverService` |
| AAA-208 | 特殊傷害防護解析 | 已抽出 `MatchSpecialDamagePreventionResolverService` |
| AAA-209 | Passive Gift 受傷減免解析 | 已抽出 `MatchPassiveGiftIncomingDamageReductionResolverService` |
| AAA-210 | Attached Support 受傷減免解析 | 已抽出 `MatchAttachedSupportIncomingDamageReductionResolverService` |
| AAA-211 | Passive Gift HP 變動防護解析 | 已抽出 `MatchPassiveGiftHpChangePreventionResolverService` |
| AAA-212 | Passive Gift 藝能加成解析 | 已抽出 `MatchPassiveGiftArtBonusResolverService` |
| AAA-213 | Passive Gift 藝能費用減免解析 | 已抽出 `MatchPassiveGiftArtCostReductionResolverService` |
| AAA-214 | 藝能文字傷害加成解析 | 已抽出 `MatchArtTextDamageBonusResolverService` |
| AAA-215 | 藝能擊倒後效果執行 | 已抽出 `MatchArtDownTriggeredEffectExecutionService` |
| AAA-216 | 受擊觸發 Gift 防護執行 | 已抽出 `MatchTriggeredGiftDamagePreventionExecutionService` |
| AAA-217 | 觸發戰鬥效果 art down bridge cleanup | `MatchTriggeredCombatEffectService` 直接持有 `MatchArtDownTriggeredEffectExecutionService` |
| AAA-218 | 效果文案分類解析 | 已抽出 `MatchEffectTypeInferenceService` |
| AAA-219 | Gift effect 執行摘要協調 | 已抽出 `MatchGiftEffectExecutionCoordinator` |
| AAA-220 | Gift effect type 分派 | 已抽出 `MatchGiftEffectDispatcher` |
| AAA-221 | Gift archive 回手效果執行 | 已抽出 `MatchGiftArchiveReturnEffectExecutionService` |
| AAA-222 | Gift reattach 效果執行 | 已抽出 `MatchGiftReattachEffectExecutionService` |
| AAA-223 | Bloom / Collab reattach bridge cleanup | Bloom / Collab dispatcher 直接依賴 `MatchGiftReattachEffectExecutionService` |
| AAA-224 | Gift add cheer 效果執行 | 已抽出 `MatchGiftAddCheerEffectExecutionService` |
| AAA-225 | Add cheer bridge cleanup | Bloom / Collab / Gift / Support 入口直接依賴 `MatchGiftAddCheerEffectExecutionService` |
| AAA-226 | Add cheer 目標解析 | 已抽出 `MatchAddCheerTargetResolverService` |

---

## 三、目前尺寸

- `src/main/java/com/hololive/cardgame/service/MatchEffectService.java`：`4,910` 行。
- `src/main/java/com/hololive/cardgame/service/MatchActionService.java`：`6,099` 行。
- `src/test/java/com/hololive/cardgame/service/MatchActionServiceIntegrationTest.java`：`32,302` 行。

---

## 四、下一步優先序

### 1. 繼續細分 `DAMAGE` 周邊高耦合 resolver

原因：

- `MatchDamageEffectExecutionService` 已承接 target resolution 後的 deterministic damage execution、summary、down 歸檔與 life loss orchestration。
- `MatchDamageEffectiveHpResolverService` 已承接 base HP、attached support HP bonus、self passive Gift HP bonus 快照解析。
- `MatchSpecialDamagePreventionResolverService` 已承接 SPECIAL_DAMAGE_IMMUNITY 查詢與 HSD13-012 stack cost 防護啟用。
- `MatchPassiveGiftIncomingDamageReductionResolverService` 已承接 passive Gift 受傷減免 target/holder 查詢、來源等級限制、骰子減免與 turn usage 紀錄。
- `MatchAttachedSupportIncomingDamageReductionResolverService` 已承接 attached support 受傷減免查詢與 base 文案解析。
- `MatchPassiveGiftHpChangePreventionResolverService` 已承接「相手能力不能改變 HP」的回合、target、holder 與條件解析。
- `MatchPassiveGiftArtBonusResolverService` 已承接 passive Gift 藝能加成 target/holder 查詢、特殊傷害加成、holder / target 條件與歷史推し技能條件解析。
- `MatchPassiveGiftArtCostReductionResolverService` 已承接 passive Gift 藝能費用減免 target/holder 查詢、art name、cheer color、attached support 與歷史推し技能條件解析。
- `MatchArtTextDamageBonusResolverService` 已承接藝能文案自體加傷 target context、Cheer / LIFE / 推し技能歷史 / 指定推し條件解析。
- `MatchArtDownTriggeredEffectExecutionService` 已承接藝能擊倒後 follow-up raw text 擷取、clause 解析、no-op summary 與 support effect 包裝。
- `MatchTriggeredGiftDamagePreventionExecutionService` 已承接 DAMAGE_RECEIVED Gift 受傷防護的 target / holder 查詢、turn usage、target / collab / dice 條件與 PREVENT_DAMAGE summary 組裝。
- `MatchTriggeredCombatEffectService` 已直接持有 `MatchArtDownTriggeredEffectExecutionService`，移除 `MatchEffectService.applyArtDownTriggeredEffects(...)` bridge。
- `MatchEffectTypeInferenceService` 已承接 Bloom / Collab / Gift / ArtDown 共用 effect type 與 target type 文案分類，dispatcher 與 triggered combat 不再透過 `MatchEffectService` 取得 target type 推斷。
- `MatchGiftEffectExecutionCoordinator` 已承接 Gift sequential-cost 判斷、cost / resolved effect 執行順序與 executed / unsupported / skipped summary 組裝。
- `MatchGiftEffectDispatcher` 已承接 Gift effect type switch routing；`MatchGiftEffectServiceHandlers` 只保留高耦合副作用 callback adapter。
- `MatchGiftArchiveReturnEffectExecutionService` 已承接 HBP02-039 / `REPLACE_ARCHIVE_WITH_HAND` 的 latest Holox `ATTACK_ART` payload 查詢、candidate summary、Archive -> Hand 移動與 no-op reason。
- `MatchGiftReattachEffectExecutionService` 已承接 `REATTACH` 的 dice / owner / target / source mode / Cheer row 搬移與 summary 組裝；Gift handler 已直接委派新 service。
- Bloom / Collab dispatcher 已直接持有 `MatchGiftReattachEffectExecutionService`，移除 `MatchEffectService.executeReattachEffect(...)` 薄 delegate。
- `MatchGiftAddCheerEffectExecutionService` 已承接 `ADD_CHEER` 的 raw text / resolved clause、`SendCheerAction` 執行、SQL fallback 與 summary 組裝。
- Bloom / Collab dispatcher、Gift handler 與 Support effect 入口已直接依賴 `MatchGiftAddCheerEffectExecutionService`，移除 `MatchEffectService.executeAddCheerEffect(...)` 薄 delegate。
- `MatchAddCheerTargetResolverService` 已承接 `ADD_CHEER` 目標子句擷取、區位 / 等級 / 名稱 / tag 條件、owned stage candidate 查詢與 restricted target no-fallback 規則。
- `MatchAddCheerSourceResolverService` 已承接 `ADD_CHEER` 來源子句擷取與 Archive / Cheer deck 來源選擇。
- `MatchGiftAddCheerEffectExecutionService` 已直接持有 `MatchAddCheerSourceResolverService` 與 `MatchAddCheerTargetResolverService`，移除 ADD_CHEER execution 專用 source / target functional callback interface。
- `MatchCheerCandidateQueryService` 已承接 `REATTACH` 與 `ADD_CHEER` source resolver 共用的 Cheer zone candidate 查詢，以及 `ADD_CHEER` 未指定來源時的 CHEER_DECK > ARCHIVE > HAND fallback attachable Cheer 查詢。
- 下一批若繼續處理 Gift，建議收斂 `MatchGiftReattachEffectExecutionService` 的 target resolver callback，讓 reattach 直接依賴 `MatchAddCheerTargetResolverService`；若暫停 Gift，可回頭處理 `MatchActionService` decision / turn lifecycle。

建議下一個 commit：

```text
後端：收斂 Reattach 目標解析依賴
```

建議讓 `MatchGiftReattachEffectExecutionService` 直接持有 `MatchAddCheerTargetResolverService`，移除 reattach 內剩餘的 `PreferredAddCheerTargetResolver` callback。

這批不改 `REATTACH` 規則語意，只把 target resolver wiring 從 method reference 改成直接 service dependency。完成後 Gift / Add Cheer / Reattach 的 execution-service dependency shape 會更一致。

### 2. 回頭整理 `MatchActionService`

原因：

- `MatchEffectService` 已連續完成多批 execution family extraction。
- `MatchActionService` 仍有 turn / phase lifecycle、decision resolution flow、board action orchestration 等高密度區塊。

### 3. 暫緩測試巨檔治理

暫不優先處理：

- `MatchActionServiceIntegrationTest`

目前只有在 production 重構需要保護測試或測試定位困難時，才順手拆對應主題測試。

---

## 五、驗證基準

每批 production 重構至少執行：

```bash
./mvnw -q -Dtest=<NewServiceTest> test
./mvnw -q -DskipTests compile
./mvnw -q -Dtest=MatchActionServiceIntegrationTest#<focused-case> test
git diff --check
```

若 integration test 因 Testcontainers / Docker / PostgreSQL socket 權限失敗，依使用者授權提高權限重跑，並在 acceptance review 中記錄。

---

## 六、文件維護規則

- 本檔只保留目前進度摘要與近期方向。
- 詳細歷史請查 `doc/系統優化/archive/05-重構進度追蹤-完整歷史-2026-05-25.md`。
- 每批重構完成後新增 acceptance review，並更新本檔摘要。
- 不再把所有歷史步驟持續追加到本檔，避免它再次膨脹到萬行級文件。
